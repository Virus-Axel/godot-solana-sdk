// Story 2-2 T2 — grep-ban pattern-9 self-test fixture (DD-2-2-1 LOCKED).
// This file is INTENTIONALLY a violation. The self-test asserts the pattern fires.
// DO NOT reference this file from production code.

// Variant A: symbolic identifier 'cluster_mismatch' (DD-2-2-1 anti-pattern)
fun badReauth_variantA(cachedRecord: Any, cluster: String) {
    if (cluster_mismatch) {
        emitFailure("NOT_CONNECTED")
    }
}

// Variant B: explicit cluster != activeCluster comparison (DD-2-2-1 anti-pattern)
fun badReauth_variantB(cluster: String, activeCluster: String) {
    if (cluster != activeCluster) {
        emitFailure("NOT_CONNECTED")
    }
}

// Variant C: cachedRecord.cluster comparison (DD-2-2-1 anti-pattern)
fun badReauth_variantC(cachedRecord: CacheRecord, cluster: String) {
    if (cachedRecord.cluster != cluster) {
        emitFailure("NOT_CONNECTED")
    }
}
