<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="false"
        android:fullBackupContent="@xml/backup_rules"
        android:dataExtractionRules="@xml/data_extraction_rules"
        tools:replace="android:allowBackup">
        <!--
        ComposeWalletActivity is launched ONLY by the plugin itself via explicit
        Intent(ctx, ComposeWalletActivity::class.java) (see GDExtensionAndroidPlugin.kt
        lines 1533, 1562, 1572). It declares no <intent-filter> and is not addressed
        by any external caller (Story 4-3 T3 grep verified zero external referrers).
        Per AC-3 we therefore omit android:exported (defaults to false on API 31+ since
        there is no intent-filter, and explicit-only callers within the same UID can
        still reach it). taskAffinity="" + launchMode="singleTask" prevent the
        Activity from being placed in the host app's back-stack (AC-3 hardening).
        -->
        <activity
            android:name=".ComposeWalletActivity"
            android:theme="@style/TransparentActivityTheme"
            android:taskAffinity=""
            android:launchMode="singleTask"/>
        <!--
        Plugin metadata:

        - In the `android:name` attribute, the `org.godotengine.plugin.v2` prefix
        is required so Godot can recognize the project as a valid Godot
        Android plugin. The plugin name following the prefix should match the value
        of the plugin name returned by the plugin initializer.

        - The `android:value` attribute should be the classpath to the plugin
        initializer.
        -->
        <meta-data
            android:name="org.godotengine.plugin.v2.${godotPluginName}"
            android:value="${godotPluginPackageName}.GDExtensionAndroidPlugin"/>
    </application>

</manifest>
