<!--
Quality Signal: Inlined 8 sections (A-17 amendment summary verbatim from amendments.md:339+; AC-D-12 .gdextension drift from arch:1147; AC-NFR-SEC-10 verbatim from spec.md:188; FR-1 verbatim from spec.md:53; AC-3 verbatim from plan.md:420; identity_uri canonical placeholder `https://example.com/godot-mwa-sample` from arch:207 + spec:188; addon-bundle layout from plan.md 5-6 AC-1 `scons addon` produces addons/SolanaSDK/ with GDExtension binaries + AAR + GDScript + docs; pending_submission_found one-shot per Story 3-3 DD-3-3-D as design guidance for the external repo's connect handler). Refs 7 (docs/amendments.md A-17 full architectural-relocation amendment + DD-5-4-5 release-tag mechanism + CR-5-4-D/E/F concerns; docs/stories/5-3.md DD-5-3-4 mwa_cancelled_lifecycle reason vocabulary as design guidance for external repo Cancel button; docs/stories/3-3.md DD-3-3-D + DD-3-3-E pending_submission_found contract; docs/architecture.md §2.2 component M + §3.2 12-signal payloads + line 1147 .gdextension drift; addons/SolanaSDK/mwa/MWA.gd:27-47 + :133-418 — single source of truth for the addon API surface that the external repo's example will consume; godot-solana-sdk.gdextension:4 T0 fix-target; existing GitHub Releases workflow gh CLI for `v0.1.0-rc1` cut at T2). Omitted: full GDScript scene + handler skeletons (relocated to external repo `ValentinVPK/godot-mwa-example`; this repo no longer ships example/MWA/), `tools/review-demo.sh` body (relocated to external repo), `_race_signals` helper body (relocated to external repo with design-guidance pointer in REVIEW.md only), Storybook/CI workflow rewrites for instrumented_emulator_fakewallet.yml (deferred to Story 5-6 per CR-5-4-D), Godot scene file format binary vs text (4.3 default text — discoverable from existing addons/SolanaSDK/Scenes/), Android adb / shell idioms (standard Android SDK surface).

Budget: M (target 250-350 lines per A-17). Story 5-4 is REDUCED-SCOPE under A-17 (Rule 4 architectural relocation): the example app moves to external repo `ValentinVPK/godot-mwa-example`. This repo's 5-4 deliverables shrink to (a) close AC-D-12 .gdextension drift, (b) author REVIEW.md pointing reviewers at the external repo + tagged-release flow, (c) cut first release tag `v0.1.0-rc1`. ACs 1, 2, 4, 5, 6 migrate OUT-OF-LAIM-SCOPE per A-17; only AC-3 (REVIEW.md) remains in-scope with rewritten content. 3 LOCKED design decisions in this revision: DD-5-4-1 (.gdextension drift fix — kept), DD-5-4-5 (tagged-release consumption mechanism — added). DD-5-4-2 / DD-5-4-3 / DD-5-4-4 are DEMOTED from in-tree LaiM-tracked deliverables to DESIGN GUIDANCE that REVIEW.md hands to the external-repo implementer.

Live state confirmed at story-creation revision (post-A-17):
- post-5-3 Gate 5 PASS at `a5b6c54b`; post-5-2 Gate 5 PASS (Wave 7 3/6 complete: 5-1, 5-2, 5-3 done; 5-4 starting REDUCED scope per A-17; 5-5 + 5-6 backlog).
- baseline 277 GREEN at story-creation revision (refreshed 2026-04-29T11:30Z post Story 5-2 T6 close-out per state.json `baselineTests.lastRefreshedAt`).
- pre-tag `pre-mwa-android-5-4` already at `39ff0d1f` per sprint-status.yaml + state.json bookkeeping.
- AC-D-12 STILL OPEN: `godot-solana-sdk.gdextension:4` reads `compatibility_minimum = "4.4"` despite arch:1147 noting the correction was supposed to land in Story 1-2. DD-5-4-1 LOCKS T0 fixes the drift. Pre-edit grep verified at story-creation 2026-04-29.
- A-17 authored 2026-04-29 at story-creation revision HALT (Rule 4 architectural change with explicit user approval).
- External repo name: `ValentinVPK/godot-mwa-example` (resolved at story-creation revision 2026-04-29 by user direction; the repo itself is bootstrapped by the user post-Gate-5 per CR-5-4-E).
- Open concerns added by A-17: CR-5-4-D (Story 5-6 instrumented_emulator_fakewallet.yml redesign — MEDIUM), CR-5-4-E (external-repo AC verification gap — HIGH), CR-5-4-F (release-on-tag automation deferred — LOW). All logged at T-final close-out.
-->
---
id: "5-4"
slug: example-app-review-md
title: "REVIEW.md + .gdextension Drift Fix + First Addon Release Tag (example app relocates to external repo per A-17)"
epic: 5
wave: 7
size: M
status: ready
dependencies: ["2-1", "2-2", "2-3", "3-1", "3-2", "3-3", "4-1"]
baselineTag: pre-mwa-android-5-4
created: 2026-04-29
revised: 2026-04-29
amendment: A-17
---

# Story 5-4: REVIEW.md + .gdextension Drift Fix + First Addon Release Tag

> **REDUCED SCOPE per A-17 (`docs/amendments.md`).** The minimal example app originally specified at `example/MWA/` relocates to a separate public repository `ValentinVPK/godot-mwa-example` per user direction at story-creation HALT. ACs 1, 2, 4, 5, 6 migrate OUT-OF-LAIM-SCOPE — they are implemented + verified in the external repo, NOT in this LaiM run. AC-3 (REVIEW.md) stays in-scope but its content is rewritten around the clone-external + extract-release-zip flow.

## User Story
*(verbatim from `docs/plan.md:411` — preserved for spec lineage; the deliverable shape changes per A-17, but the user-need framing is unchanged)*

As a grant reviewer, I want a minimal example Godot Android app demonstrating all 7 MWA methods on devnet, with a `REVIEW.md` that takes me from clone to running demo in ≤10 minutes, so that I can verify API parity without being an Android/Godot expert.

## Context

- **A-17 architectural relocation in effect.** Per user direction recorded in `docs/amendments.md:A-17`, the example app moves to a separate public repository `ValentinVPK/godot-mwa-example`. Three resolution paths were considered ((A) Defer, (B) Hybrid, (C) Full move); (C) was chosen because the FR-1 audit ("install single addon, no Java/Kotlin/C++ required") becomes a TRUE downstream-consumer test under (C) rather than a same-tree shortcut. (B) Hybrid would have masked the very failure modes FR-1 exists to surface.
- **Story 5-4 in this repo is now a 3-task, ~250-350-line story.** T0 closes the AC-D-12 `.gdextension compatibility_minimum` drift; T1 authors `REVIEW.md` at repo root (clone-external + release-zip recipe); T2 cuts the first release tag `v0.1.0-rc1` from current master and runs close-out.
- **AC-D-12 .gdextension drift IS STILL OPEN.** Architecture line 1147 says: "compatibility_minimum = `"4.4"` in the gdextension file is an upstream inconsistency we will correct to `"4.3"` as part of the scaffold drift cleanup (AC-D-12)" — but the correction was never executed in Story 1-2. Verified at story-creation: `godot-solana-sdk.gdextension:4` STILL contains `compatibility_minimum = "4.4"`. Godot 4.3.x cannot load the addon as-is. **DD-5-4-1 LOCKS: T0 fixes the drift in this story** (Rule-3 deviation per A-17's "Closes" line; the close-out commit SHA goes into A-17's text at T-final).
- **DD-5-4-5 LOCKS the addon consumption mechanism: tagged release.** External repo `ValentinVPK/godot-mwa-example` consumes `addons/SolanaSDK/` from THIS repo via GitHub Releases (published artifact ZIP), NOT via git submodule (build-artifact problem) and NOT via vendor copy (drift + hides SDK from grant reviewers). T2 cuts `v0.1.0-rc1` from current master after T0 + T1 land; full release-on-tag automation is deferred to Story 5-6 per CR-5-4-F.
- **AC-NFR-SEC-10 (`spec.md:188`) constraints preserved as DESIGN GUIDANCE.** Verbatim from spec: "Example app defaults to `devnet`; mainnet-beta toggle requires explicit config + in-app confirmation dialog; `identityUri` is placeholder (`https://example.com/godot-mwa-sample`), not a real partner domain." REVIEW.md hands these constraints to the external-repo implementer; this repo's 5-4 does NOT verify them in Gate 5 (CR-5-4-E).
- **Wave 7 cluster context — divide-of-labor with 5-5 and 5-6 under A-17.** Story 5-5 (Documentation Bundle) ships `docs/install.md` (full developer reference) — distinct from 5-4's `REVIEW.md` (reviewer-only ≤10-min quick-path). Story 5-5 should also reference both repos (this SDK repo + the external example repo). Story 5-6 (CI/Build Integration) must redesign `instrumented_emulator_fakewallet.yml` per CR-5-4-D's three options (workflow stays here cloning external; workflow moves to external; or workflow becomes release-gate-only).

## Acceptance Criteria
*(verbatim from `docs/plan.md:418-425` — preserved for lineage; in-scope status changes per A-17)*

- **AC-1 (scene):** *Given* the new example at `example/MWA/`, *when* opened in Godot 4.3.x, *then* it contains a single scene `main.tscn` with labeled buttons `[Connect] [Disconnect] [Reauthorize] [Deauthorize] [Sign Message] [Sign Transaction] [Sign and Send] [Reset Local State] [Forget All]`, a `RichTextLabel` log area, and a "Cluster: devnet" label; scene runs without errors in the editor.
  - **Status:** OUT-OF-LAIM-SCOPE per A-17. Verified in `ValentinVPK/godot-mwa-example`'s own CI (a `godot --headless --check-only main.tscn` smoke or equivalent). REVIEW.md hands the button-list verbatim as a spec for the external-repo implementer.
- **AC-2 (devnet default):** *Given* the example's config script, *when* inspected, *then* `cluster = "devnet"` is the hard-coded default; switching to `"mainnet-beta"` requires editing a const AND clicking an in-app "I understand — use mainnet" confirm dialog on first use; `identity_uri = "https://example.com/godot-mwa-sample"` is the placeholder (not a real domain).
  - **Status:** OUT-OF-LAIM-SCOPE per A-17. Verified in external repo. REVIEW.md preserves the AC-2 + AC-NFR-SEC-10 constraints as DESIGN GUIDANCE (devnet default + per-launch confirmation gate per former DD-5-4-4 + canonical identity_uri placeholder).
- **AC-3 (REVIEW.md):** *Given* `REVIEW.md` at repo root, *when* a reviewer follows it, *then* they can: (1) install Android SDK + NDK via documented commands, (2) run `scons addon && cd android && ./gradlew :plugin:assembleRelease`, (3) install the Fake Wallet APK + example APK to an emulator via `adb install`, (4) run a scripted flow (`tools/review-demo.sh`) that drives the example through all 7 methods, printing `PASS: <method>` for each; total wall-clock ≤10 min on a modern laptop with warm caches.
  - **Status:** IN-SCOPE in this repo, content REWRITTEN per A-17. The new flow reviewers follow:
    1. Clone `ValentinVPK/godot-mwa-example`.
    2. Open this repo's GitHub Releases page; download `v0.1.0-rc1` (or current latest) `addons-bundle.zip` release asset.
    3. Extract into the external repo's `addons/SolanaSDK/` (per FR-1 single-addon-install pattern).
    4. Install Fake Wallet APK to an emulator via `adb install` (Fake Wallet APK source is documented in REVIEW.md — it lives in this repo's `tests/instrumented/fixtures/` per Story 1-6 + 2-1 conventions, OR linked from upstream `solana-mobile/mobile-wallet-adapter` per its existing convention; T1 verifies which path is current).
    5. Build + install the external-repo example app per its README.
    6. Run the external repo's `tools/review-demo.sh` (the script lives in the external repo, NOT here) — script drives the demo through all 7 methods + asserts 7/7 PASS markers via `adb pull` + `grep -c '|PASS|'` against the marker log file.
  - **Evidence path in this repo:** T1 lands `REVIEW.md` at repo root with the above flow. T2 close-out runs a markdown link-check (`grep -nE 'https?://github\.com/.*/godot-mwa-example' REVIEW.md` returns the external repo URL; `grep -nE 'releases/tag/v[0-9]' REVIEW.md` returns the release-tag reference) AND a manual reviewer-walkthrough sanity pass against the local checkout.
  - **The 10-min wall-clock SLO** is a SOFT target in 5-4 (no automated wallclock assertion). Story 5-6's CI SLO monitor (per plan.md 5-6 AC-4) is the long-term enforcement; under A-17 + CR-5-4-D, that monitor's redesign is deferred to 5-6.
- **AC-4 (accessible error display):** *Given* any `mwa_error` signal, *when* the example app renders it, *then* the log area shows the `user_message` as TEXT (not color alone); buttons have accessible names (`AccessibleName` property set); contrast passes WCAG AA against the default Godot theme.
  - **Status:** OUT-OF-LAIM-SCOPE per A-17. Verified in external repo. REVIEW.md preserves the AC-4 constraints as DESIGN GUIDANCE: render `payload["user_message"]` as TEXT (color is decoration, not substrate); set `Button.accessible_name` (snake_case GDScript property; `⚠ VERSION NOT VERIFIED` — the external implementer must web-search the Godot 4.3.x point release that landed it, OR fall back to setting via the `.tscn` inspector); default Godot dark theme passes WCAG AA at 4.5:1 normal / 3:1 large by-construction.
- **AC-5 (await+error race):** *Given* the example's connect button handler, *when* inspected, *then* the code uses the `await` + error-signal race pattern (helper function races success vs error, caller awaits only the helper); a cancel button invokes a documented "cancel pending op" path.
  - **Status:** OUT-OF-LAIM-SCOPE per A-17. Verified in external repo. REVIEW.md preserves the AC-5 design as GUIDANCE: ship `_race_signals(success_sig: Signal, error_sig: Signal) -> Variant` helper at `<example-repo>/scripts/await_helpers.gd` (returns `{ok: true, value: <success payload>}` or `{ok: false, error: <error payload>}`); all 7 op handlers call it; the Cancel button invokes `MWA.forget_all()` (Story 4-2's `forget_all` is the documented cancel-in-flight path emitting `mwa_cancelled_lifecycle{reason: "forget_all_invoked"}`); helper is example-internal — NOT shipped in `addons/SolanaSDK/` to keep the public addon API minimal for FR-1 audit + Story 5-5 docs canonicalization.
- **AC-6 (state transition log):** *Given* any operation, *when* it runs, *then* the example logs `[<timestamp>] <method>: <state>` for state transitions (started → wallet-prompt-open → wallet-returned → signal-fired); reviewer can follow the flow linearly.
  - **Status:** OUT-OF-LAIM-SCOPE per A-17. Verified in external repo. REVIEW.md preserves the AC-6 design as GUIDANCE: log shape `[<ISO timestamp>] <method>: <state>`; substates `started` / `wallet-prompt-launched` / `wallet-returned` / `signal-fired` (the `wallet-prompt-launched` and `wallet-returned` substates are coarse-grained — Godot does NOT have direct hooks into Android Activity transitions; log `wallet-prompt-launched` immediately after the `MWA.<op>(...)` call returns and `wallet-returned` when ANY terminal signal fires); PASS-marker mechanism per former DD-5-4-2 — write `<ISO timestamp>|<method>|PASS|<correlation_id>` to `OS.user_data_dir() + "/review-pass.log"` from each success-signal handler; the external repo's `tools/review-demo.sh` does `adb pull` + `grep -c '|PASS|'` to verify 7/7.

### AC → Task Mapping
| AC | Status (per A-17) | Covered By Tasks (in this repo) |
|----|-------------------|----------------------------------|
| AC-1 | OUT-OF-LAIM-SCOPE | T0 (.gdextension fix is the prerequisite for "opens in Godot 4.3.x" — without T0 the external-repo example cannot load the addon at all) |
| AC-2 | OUT-OF-LAIM-SCOPE | DESIGN GUIDANCE in REVIEW.md (T1) |
| AC-3 | IN-SCOPE (rewritten) | T1 (REVIEW.md authoring), T2 (link-check + manual walkthrough) |
| AC-4 | OUT-OF-LAIM-SCOPE | DESIGN GUIDANCE in REVIEW.md (T1) |
| AC-5 | OUT-OF-LAIM-SCOPE | DESIGN GUIDANCE in REVIEW.md (T1) |
| AC-6 | OUT-OF-LAIM-SCOPE | DESIGN GUIDANCE in REVIEW.md (T1) |
| (DD-5-4-5 release tag) | NEW per A-17 | T2 (`gh release create v0.1.0-rc1` with `addons-bundle.zip` asset) |

## Architecture Guardrails

3 LOCKED design decisions in the post-A-17 revision: DD-5-4-1 (kept — .gdextension drift fix), DD-5-4-5 (added — addon consumption via tagged release). Former DD-5-4-2 (PASS marker file format), DD-5-4-3 (`_race_signals` helper location), DD-5-4-4 (per-launch mainnet confirmation) are DEMOTED to DESIGN GUIDANCE handed to the external-repo implementer via REVIEW.md.

- **DD-5-4-1 (LOCKED) — `godot-solana-sdk.gdextension:4` fixed: `compatibility_minimum = "4.4"` → `"4.3"` (closes AC-D-12 originally scoped to Story 1-2).** Architecture line 1147 explicitly noted this drift was "to be corrected to `"4.3"` as part of the scaffold drift cleanup (AC-D-12)" but the fix was never executed in Story 1-2 (verified at story-creation: line 4 still reads `"4.4"`). Without this fix, Godot 4.3.x cannot load the addon — and the entire external-repo demo flow is blocked because that's the version the external example targets.
  - **File path:** `/Users/valentinkrumov/Documents/godot-sdk/godot-solana-sdk.gdextension` line 4.
  - **One-line edit:** `compatibility_minimum = "4.4"` → `compatibility_minimum = "4.3"`.
  - **Closure path:** A-17's `Closes:` line cites AC-D-12. T2 close-out updates A-17 with the actual closure commit SHA (placeholder `<closure-sha>` in the amendment text gets replaced post-T0 commit).
  - **Rejected alternative:** defer to a follow-up story — blocks the external repo's ability to load the addon entirely; the fix is a 1-line edit + amendment-text SHA update.

- **DD-5-4-5 (LOCKED per A-17) — addon consumption via GitHub tagged release; `v0.1.0-rc1` cut from current master at T2.** External repo `ValentinVPK/godot-mwa-example` consumes `addons/SolanaSDK/` via release ZIP (NOT submodule; NOT vendor copy). Rationale fully recorded in `docs/amendments.md:A-17`.
  - **First release at T2:** `gh release create v0.1.0-rc1 addons-bundle.zip --title "v0.1.0-rc1 (pre-Gate-5 snapshot for godot-mwa-example bootstrap)" --notes "First release cut from Story 5-4 T2 per A-17 + DD-5-4-5. Consumed by ValentinVPK/godot-mwa-example. Promotes to v0.1.0 when this repo's Story 5-5 + 5-6 ship."`. Asset: `addons-bundle.zip` produced by `scons addon` per plan.md 5-6 AC-1 (one-command build).
  - **Release-on-tag automation:** deferred to Story 5-6 per CR-5-4-F. T2 uses manual `gh release create` for `v0.1.0-rc1`. Promotion to `v0.1.0` happens at the natural release boundary post-5-5/5-6.
  - **Rejected alternative (submodule):** native build artifacts (compiled `.so` / `.dll` / `.dylib` / `.wasm` per `scons addon`, plus AAR) are not git-tracked, so a submodule alone yields a non-functional addon — defeats FR-1's "no C++ required" promise. Submodules also recreate the FR-1 failure mode the audit is supposed to surface (first-time users forget `--recurse-submodules`).
  - **Rejected alternative (vendor copy):** hides the SDK from grant reviewers (they may assume the example IS the SDK); creates inevitable drift; "does the published addon work as advertised?" cannot be answered when there is no published addon.

## Verified Interfaces

The external repo's example app will consume the FULL MWA addon API: 14 methods (7 ops + 4 state getters + 3 utility) + 12 signals + 9-key `mwa_error` payload + 5-key `mwa_timeout` payload + 6-key `pending_submission_found` payload + 3-key `mwa_cancelled_lifecycle` payload. Source of truth: `addons/SolanaSDK/mwa/MWA.gd` (lines 27-418). REVIEW.md inlines this table as the API surface summary for the external-repo implementer + Story 5-5 docs author.

| Interface | Signature | Source | Status |
|-----------|-----------|--------|--------|
| `MWA.connect(identity: Dictionary, cluster: String, opts: Dictionary = {}) -> String` | 3-arg with `opts` defaulted; returns 8-hex `request_id` | `addons/SolanaSDK/mwa/MWA.gd:133` | ✓ VERIFIED |
| `MWA.disconnect() -> void` | 0-arg; void return; emits `disconnect_completed` | `addons/SolanaSDK/mwa/MWA.gd:141` | ✓ VERIFIED |
| `MWA.reauthorize(opts: Dictionary = {}) -> void` | 1-arg with `opts` defaulted; void return | `addons/SolanaSDK/mwa/MWA.gd:184` | ✓ VERIFIED |
| `MWA.deauthorize() -> void` | 0-arg; void return; emits `deauthorize_completed` per Story 4-1 DD-4-1-1 | `addons/SolanaSDK/mwa/MWA.gd:168` | ✓ VERIFIED |
| `MWA.sign_messages(messages: Array[PackedByteArray], opts: Dictionary = {}) -> void` | 2-arg; void return; emits `sign_messages_completed` | `addons/SolanaSDK/mwa/MWA.gd:213` | ✓ VERIFIED |
| `MWA.sign_transactions(transactions: Array[PackedByteArray], opts: Dictionary = {}) -> void` | 2-arg; void return; emits `sign_transactions_completed` | `addons/SolanaSDK/mwa/MWA.gd:246` | ✓ VERIFIED |
| `MWA.sign_and_send(transactions: Array[PackedByteArray], opts: Dictionary = {}) -> void` | 2-arg; void return; emits `sign_and_send_completed` + may emit `pending_submission_found` after next connect on prior process death | `addons/SolanaSDK/mwa/MWA.gd:300` | ✓ VERIFIED |
| `MWA.forget_all() -> void` | 0-arg; void return; NO completion signal per Story 4-2 DD-4-2-8; emits `mwa_cancelled_lifecycle{reason: "forget_all_invoked"}` per in-flight slot | `addons/SolanaSDK/mwa/MWA.gd:334` | ✓ VERIFIED |
| `MWA.is_supported() -> bool` | 0-arg; sync; true on Android only per Story 5-1 | `addons/SolanaSDK/mwa/MWA.gd:346` | ✓ VERIFIED |
| `MWA.is_connected() / .get_public_key() / .get_cluster() / .get_wallet_label() / .get_diagnostics() / .get_device_posture()` | sync state getters; documented in MWA.gd:354-408 | `addons/SolanaSDK/mwa/MWA.gd:354-408` | ✓ VERIFIED |
| `connect_completed / reauthorize_completed / disconnect_completed / deauthorize_completed / sign_messages_completed / sign_transactions_completed / sign_and_send_completed (request_id: String, result: Dictionary)` signals | 2-param per A-12 | `addons/SolanaSDK/mwa/MWA.gd:27-33` | ✓ VERIFIED |
| `mwa_error(payload: Dictionary)` signal | 1-param; 9-key payload `{request_id, code, user_message, developer_details, recoverable, retry_hint, layer, cause, source_method}` per arch §3.2:277-290 | `addons/SolanaSDK/mwa/MWA.gd:34` + `docs/architecture.md:277-290` | ✓ VERIFIED |
| `mwa_timeout(payload: Dictionary)` signal | 1-param; 4-key `{request_id, source_method, elapsed_ms, watchdog_ms}` per arch §3.2:294-298 | `addons/SolanaSDK/mwa/MWA.gd:35` | ✓ VERIFIED |
| `mwa_cancelled_lifecycle(payload: Dictionary)` signal | 1-param; 3-key `{request_id, source_method, reason}` per A-12 + Story 4-2 + 5-3; reason ∈ `{"forget_all_invoked", "activity_destroyed"}` per Story 5-3 DD-5-3-4 | `addons/SolanaSDK/mwa/MWA.gd:36` | ✓ VERIFIED |
| `reauth_required(payload: Dictionary)` signal | 1-param; payload has `{reason}` ∈ `{"keystore_corrupt"}` per Story 3-3 DD-3-3-G | `addons/SolanaSDK/mwa/MWA.gd:37` | ✓ VERIFIED (external repo listens for logging only; recovery is not tested in the demo flow) |
| `pending_submission_found(payload: Dictionary)` signal | 1-param; ONE-SHOT per Story 3-3 DD-3-3-D; 6-key `{request_id, op_type, started_at_ms, tx_count, tx_preview_hashes, recommendation}` per DD-3-3-E | `addons/SolanaSDK/mwa/MWA.gd:47` | ✓ VERIFIED — external example must subscribe via `MWA.pending_submission_found.connect(handler, CONNECT_ONE_SHOT)` in `_ready()` |
| `Button.accessible_name` (Godot 4.3+ Control property; snake_case in GDScript per property-rename convention) | Available on Button + all Control subclasses | Godot 4.3 release notes (UI accessibility track) | ⚠ VERSION NOT VERIFIED — external-repo implementer must web-search the exact 4.3.x point release that landed it (could be `accessible_name` or `accessibility_name` depending on the release) |
| `godot-solana-sdk.gdextension:4 compatibility_minimum` | currently `"4.4"`; T0 fixes to `"4.3"` per DD-5-4-1 | `/Users/valentinkrumov/Documents/godot-sdk/godot-solana-sdk.gdextension:4` | ⚠ DRIFT — T0 is the fix-task |

## Tasks

- [ ] **T0 — `.gdextension` compatibility_minimum drift fix (closes AC-D-12).**
  - Edit `/Users/valentinkrumov/Documents/godot-sdk/godot-solana-sdk.gdextension` line 4: `compatibility_minimum = "4.4"` → `compatibility_minimum = "4.3"`.
  - **Pre-edit grep:** `grep -n 'compatibility_minimum' godot-solana-sdk.gdextension` returns `4:compatibility_minimum = "4.4"`. Post-edit: `4:compatibility_minimum = "4.3"`. ✓ verified at story-creation 2026-04-29.
  - **Verification:** baseline test suite must remain GREEN (`./gradlew test` + pytest); the change is config-only, no Kotlin / pytest impact expected. Smoke: `cat godot-solana-sdk.gdextension | grep '^compatibility_minimum'` returns the new value.
  - Maps to: AC-1 (prerequisite for "opens in Godot 4.3.x" — even though AC-1 is OUT-OF-LAIM-SCOPE per A-17, the underlying drift fix is in-scope here because the addon must load on 4.3.x for the external repo's example to work)
  - Files: `godot-solana-sdk.gdextension` (modified — 1-line edit).
  - TDD: N/A (1-line config fix; no test surface)

- [ ] **T1 — Author `REVIEW.md` at repo root (clone-external + release-zip recipe + design guidance).**
  - **NEW file:** `/Users/valentinkrumov/Documents/godot-sdk/REVIEW.md`.
  - **Required sections (in this order):**
    1. **`# REVIEW.md — Grant Reviewer Quick-Path`** — header + 1-paragraph summary describing the ≤10-min clone-to-demo flow. Note that the example app lives in `ValentinVPK/godot-mwa-example`, NOT in this repo (per A-17).
    2. **`## Prerequisites`** — Godot 4.3.x editor; Android device or emulator (API 23+); Fake Wallet APK (link / source documented at T1 time — verify whether it lives in this repo's `tests/instrumented/fixtures/` per Story 1-6 + 2-1, OR is sourced from upstream `solana-mobile/mobile-wallet-adapter`'s Fake Wallet build per its existing release pattern; choose whichever path is current and document it).
    3. **`## Step 1 — Clone the example repo`** — `git clone https://github.com/ValentinVPK/godot-mwa-example.git && cd godot-mwa-example`.
    4. **`## Step 2 — Download this repo's addon release`** — point reviewer at this repo's GitHub Releases page (`https://github.com/<this-org>/<this-repo>/releases/tag/v0.1.0-rc1` — fill in actual URL at T1 time once the org / repo segment is known); instruct them to download `addons-bundle.zip` and extract into the external repo's `addons/SolanaSDK/`.
    5. **`## Step 3 — Install Fake Wallet`** — `adb install <fake-wallet-apk>` (path or upstream URL per Step 1's prerequisites).
    6. **`## Step 4 — Run the demo`** — open the external repo's project in Godot 4.3.x → build via the external repo's README → install onto emulator → run external repo's `tools/review-demo.sh` from inside the cloned external repo (script lives there, NOT here).
    7. **`## What to look for (FR-1 / FR-16 audit)`** — checklist:
       - [ ] Addon loads on Godot 4.3.x without "incompatible godot version" errors (T0 must be in the addon release that the external repo consumes).
       - [ ] All 9 buttons in the external repo's main scene render with accessible names visible to a screen reader (AC-4).
       - [ ] Cluster label reads "devnet" by default (AC-2).
       - [ ] Switching to mainnet-beta requires both a const-edit AND an in-app `"I understand — use mainnet"` confirmation (AC-2).
       - [ ] `tools/review-demo.sh` (in external repo) prints exactly 7 PASS markers — one per MWA op (AC-3).
       - [ ] Total wall-clock from clone to PASS-x7 ≤ 10 min on a modern laptop with warm caches (AC-3 SLO).
    8. **`## Design guidance for the example-repo implementer`** — DESIGN GUIDANCE preserved from former DD-5-4-2/3/4 (per A-17):
       - **PASS marker shape (former DD-5-4-2):** write `<ISO timestamp>|<method>|PASS|<correlation_id>` to `OS.user_data_dir() + "/review-pass.log"` from each `*_completed` handler; correlation_id is the 8-hex `request_id` per Story 1-5 D-4. `tools/review-demo.sh` does `adb pull` + `grep -c '|PASS|'`. ZERO PII (no auth_token / pubkey).
       - **`_race_signals` helper (former DD-5-4-3):** ship at `<example-repo>/scripts/await_helpers.gd`; signature `static func _race_signals(success_sig: Signal, error_sig: Signal) -> Dictionary` returning `{ok: true, value}` or `{ok: false, error}`. Example-internal — do NOT contribute back to `addons/SolanaSDK/`.
       - **Mainnet confirmation per-launch (former DD-5-4-4):** confirmation is session-only (not persisted to `user://`) — fresh attention-checkpoint each launch.
       - **Cancel button (AC-5):** invoke `MWA.forget_all()` — that's the documented cancel-in-flight path emitting `mwa_cancelled_lifecycle{reason: "forget_all_invoked"}` per Story 4-2.
       - **`pending_submission_found` (Story 3-3 DD-3-3-D):** subscribe one-shot in `_ready()`: `MWA.pending_submission_found.connect(handler, CONNECT_ONE_SHOT)`.
       - **Button text + accessible_name verbatim from AC-1:** `[Connect]` / `[Disconnect]` / `[Reauthorize]` / `[Deauthorize]` / `[Sign Message]` / `[Sign Transaction]` / `[Sign and Send]` / `[Reset Local State]` / `[Forget All]` (9 buttons; `[Reset Local State]` calls `MWA.disconnect()` per AC-U-5 — `MWA.clear_local_cache()` is documented but not on the GDScript facade today; CR-5-4-A applies).
    9. **`## API surface (consumed by the external repo)`** — inline the Verified Interfaces table from this story file verbatim, so the external-repo implementer + Story 5-5 docs author have a single canonical reference.
    10. **`## CI status`** — link to the external repo's CI status badge + workflow runs page (REVIEW.md must explicitly state that ACs 1, 2, 4, 5, 6 are verified by the external repo's own CI per A-17 + CR-5-4-E; reviewers should not trust those ACs unless that CI is green).
  - **Verification:** at T2 close-out: (a) markdown link-check via `grep -nE 'https?://github\.com/ValentinVPK/godot-mwa-example' REVIEW.md` returns ≥1 line (external-repo URL referenced); (b) `grep -nE 'releases/tag/v[0-9]' REVIEW.md` returns ≥1 line (release-tag reference); (c) manual reviewer-walkthrough sanity pass — author reads the file end-to-end without hitting any "wait, what does X mean?" friction points.
  - Maps to: AC-3 (in-scope, rewritten content)
  - Files: `REVIEW.md` (new, repo root).
  - TDD: N/A (documentation; verification is structural greps + manual sanity pass)

- [ ] **T2 — Cut first release tag `v0.1.0-rc1` + close-out (link check, baseline refresh, A-17 SHA fill-in, concerns log).**
  - **Step A: produce release artifact.** Run `scons addon` per plan.md 5-6 AC-1 from current master. Verify `addons-bundle.zip` (or equivalent — the actual artifact name produced by `scons addon` is verified at T2 time; if `scons addon` does not produce a single ZIP today, T2 packages `addons/SolanaSDK/` into a ZIP via `cd addons && zip -r ../addons-bundle.zip SolanaSDK/`). If the addon-bundle production step is missing or differs from plan.md 5-6 AC-1's wording, log a deviation per Rule 1 (`D-5-4-T2-1`) and proceed with the manual zip.
  - **Step B: cut the release tag.** `gh release create v0.1.0-rc1 addons-bundle.zip --title "v0.1.0-rc1 (pre-Gate-5 snapshot for godot-mwa-example bootstrap)" --notes "First release cut from Story 5-4 T2 per A-17 + DD-5-4-5. Consumed by ValentinVPK/godot-mwa-example. Promotes to v0.1.0 when this repo's Story 5-5 + 5-6 ship."` Pre-flight: `gh auth status` confirms a working CLI session; `gh repo view --json url` confirms the target repo is the current one. If `gh` is not installed or auth is not in place at T2 time, halt T2 and present to user (`[I] Install gh + auth then resume / [M] Manual release via web UI / [D] Defer release-cutting to user post-gate-5`).
  - **Step C: close out the amendment.** Update `docs/amendments.md:A-17` `Closes:` line to replace `<closure-sha>` placeholder with the actual T0 commit SHA. (If A-17 currently has the placeholder text, find-and-replace; if it does not, T2 confirms the SHA is referenced elsewhere in A-17's body.)
  - **Step D: link-check REVIEW.md.** Run the two greps documented in T1's verification block; both must return ≥1 line. Fix any issues found.
  - **Step E: log new concerns.** Append to `docs/concerns.md`:
    - `CR-5-4-D (MEDIUM)` — "Story 5-6 must redesign `instrumented_emulator_fakewallet.yml` per A-17's three options (workflow stays here cloning external repo; workflow moves to external repo; workflow becomes release-gate-only)."
    - `CR-5-4-E (HIGH)` — "External repo `ValentinVPK/godot-mwa-example` is OUT-OF-LAIM-SCOPE; ACs 1, 2, 4, 5, 6 lose this repo's Gate 5 coverage. Mitigation: REVIEW.md links external repo's CI status; user is responsible for ensuring external repo's own CI exercises ACs 1/2/4/5/6 before grant submission."
    - `CR-5-4-F (LOW)` — "First release-cutting at T2 is a single manual `gh release create v0.1.0-rc1`. Full release-on-tag automation is deferred to Story 5-6. Acceptable for grant-time delivery."
  - **Step F: refresh test baseline post-T0.** `./gradlew test && .venv/bin/pytest tools/` should remain at 277 GREEN (T0 is config-only; no test surface impact). Update state.json `baselineTests.lastRefreshedAtStory` to 19 + `lastRefreshedAt` to current ISO timestamp; `failingTests` should remain `[]`.
  - **Verification:** Step A produces the ZIP + Step B confirms `gh release view v0.1.0-rc1` returns the release with the asset attached + Step C / D / E / F all complete + baseline still 277 GREEN.
  - Maps to: AC-3 (link-check + manual walkthrough), DD-5-4-5 (release-tag cut), DD-5-4-1 (A-17 closure SHA fill-in)
  - Files: `docs/amendments.md` (A-17 SHA fill-in), `docs/concerns.md` (CR-5-4-D/E/F appended), `docs/state.json` (baseline refresh), `addons-bundle.zip` (release asset, ephemeral). NEW git tag `v0.1.0-rc1` + GitHub Release object. NO source code changes in Step C-F.
  - TDD: N/A (release-cutting + housekeeping; verification is structural assertions on `gh release view` + grep + git state)

## must_haves

EXISTS + SUBSTANTIVE + WIRED checks for goal-backward verification at Gate 5:

1. **`godot-solana-sdk.gdextension:4` reads `compatibility_minimum = "4.3"`** (T0; AC-D-12 closure). `grep -n 'compatibility_minimum' godot-solana-sdk.gdextension` returns `4:compatibility_minimum = "4.3"`.
2. **`REVIEW.md` exists at repo root with all 10 required sections** (T1; AC-3). `wc -l REVIEW.md` returns ≥80 lines (rough lower bound — ten sections of meaningful content); `grep -c '^## ' REVIEW.md` returns ≥9 (one `#` header + ≥9 `## ` subsection headers).
3. **`REVIEW.md` references the external repo URL + a release-tag URL** (T1 + T2; AC-3). `grep -nE 'https?://github\.com/ValentinVPK/godot-mwa-example' REVIEW.md` returns ≥1 line; `grep -nE 'releases/tag/v[0-9]' REVIEW.md` returns ≥1 line.
4. **`REVIEW.md` includes the Verified Interfaces API surface table** (T1; FR-1 audit groundwork). `grep -c '^| ' REVIEW.md` returns ≥17 (table rows from this story file's Verified Interfaces section).
5. **GitHub release `v0.1.0-rc1` exists with `addons-bundle.zip` (or equivalent) asset** (T2; DD-5-4-5). `gh release view v0.1.0-rc1 --json assets --jq '.assets[].name'` returns the addon bundle filename.
6. **`docs/amendments.md:A-17` `Closes:` line includes the T0 closure commit SHA** (T2; DD-5-4-1 trail). `grep -A 2 '^## A-17:' docs/amendments.md | grep -E '\*\*Closes:\*\*.*[0-9a-f]{8,}'` returns the line.
7. **`docs/concerns.md` has CR-5-4-D, CR-5-4-E, CR-5-4-F entries** (T2; A-17 follow-through). `grep -nE '^## CR-5-4-[DEF]' docs/concerns.md` returns 3 lines.
8. **Baseline test count remains 277 GREEN post-T0** (T2; no regression). `state.json baselineTests.failing == 0` and `baselineTests.total == 277`.

## Out of Scope (explicit non-goals)

- **Implementing the example app at `example/MWA/` in this repo.** The example relocates to `ValentinVPK/godot-mwa-example` per A-17. This story does NOT create `example/MWA/main.tscn` / `main.gd` / `config.gd` / `scripts/await_helpers.gd` / `scripts/review_log.gd` / `project.godot`.
- **Authoring `tools/review-demo.sh` in this repo.** The script lives in the external repo (it runs against the external repo's example app).
- **Bootstrapping `ValentinVPK/godot-mwa-example`.** That repo's creation, README, scene, handlers, helpers, CI, and `tools/review-demo.sh` are OUT-OF-LAIM-SCOPE for this LaiM run. The user owns that repo's creation post-Gate-5; CR-5-4-E tracks the external-CI obligation.
- **Story 5-6's `instrumented_emulator_fakewallet.yml` redesign.** Deferred per CR-5-4-D. Story 5-6's own design phase picks one of the three options.
- **Full release-on-tag automation.** Deferred per CR-5-4-F. T2 uses a manual `gh release create`. Story 5-6 may automate via a release workflow.
- **Promoting `v0.1.0-rc1` to `v0.1.0`.** Happens at the natural release boundary post-5-5 + 5-6 (when the SDK is feature-complete + docs-bundled + CI-hardened). Out of 5-4's scope.
- **Gate 5 verification of ACs 1, 2, 4, 5, 6.** Per A-17, those ACs are OUT-OF-LAIM-SCOPE; this repo's Gate 5 only verifies AC-3 (REVIEW.md) + DD-5-4-1 (.gdextension fix) + DD-5-4-5 (release tag). The external repo's own CI must cover the rest.
- **Mainnet ergonomics beyond the two-step gate.** Per AC-NFR-SEC-10, `identity_uri` stays as `https://example.com/godot-mwa-sample` placeholder — the external repo MUST NOT pick a real domain. Story 5-5's docs may document the upgrade path, but 5-4 does not.

## Dev Notes

- **A-17 is the source of truth.** This story file is REDUCED from the prior 572-line revision under the architectural relocation recorded in `docs/amendments.md:A-17`. The prior revision's full task structure (T0-T5 covering scene scaffolding + handlers + race helper + review-demo.sh + REVIEW.md) is preserved in git history (pre-revision commit on this branch); the design intent is preserved as DESIGN GUIDANCE in REVIEW.md (T1 step 8).
- **External repo URL.** `ValentinVPK/godot-mwa-example` is the canonical reference (resolved at story-creation revision 2026-04-29 by user direction). REVIEW.md uses this URL throughout. The repo itself is bootstrapped by the user post-Gate-5 per CR-5-4-E.
- **`Button.accessible_name` `⚠ VERSION NOT VERIFIED` carries forward to the external repo.** The external implementer must web-search the exact Godot 4.3.x point release that landed it (could be `accessible_name` or `accessibility_name`). Documented in REVIEW.md's design guidance section.
- **`MWA.clear_local_cache()` not on the GDScript facade.** Per the original 5-4 prior revision's CR-5-4-A: AC-U-5's "Reset Local State" intent on the [Reset Local State] button is satisfied by `MWA.disconnect()` as the documented stand-in. CR-5-4-A is preserved in concerns.md as a TODO for a future minor SDK version (add `MWA.clear_local_cache()` to the facade for explicit naming alignment with AC-U-5). REVIEW.md guidance handles the current naming.
- **`pending_submission_found` is one-shot.** Story 3-3's DD-3-3-D locks one-shot semantics; the external repo must subscribe via `CONNECT_ONE_SHOT` in `_ready()` to demonstrate the breadcrumb-survives-process-death path without leaking subscriptions across runs. Documented in REVIEW.md design guidance.
- **No Kotlin / C++ / scons changes in this story.** The MWA Android plugin and addon are already complete after Stories 2-1..2-3, 3-1..3-3, 4-1..4-2, 5-1..5-3. Story 5-4 (post-A-17) is documentation + 1-line config fix + one release tag. No verification chain on Kotlin or C++ source surfaces.
- **Baseline expectation.** Pre-T0: 277 GREEN. Post-T0: 277 GREEN (T0 is config-only). Post-T1: 277 GREEN (T1 is documentation-only). Post-T2: 277 GREEN (T2 is housekeeping + release tag — no source surface). Any deviation from this expectation is a regression and triggers VERIFY.md §3d.1's baseline-comparison gate.
- **Story 5-6 must read A-17 before its own design phase.** CR-5-4-D's three options for `instrumented_emulator_fakewallet.yml` are non-trivial; 5-6's spec should explicitly reference A-17 + DD-5-4-5 as inputs.
- **Story 5-5 must read A-17 + REVIEW.md before its own docs phase.** The example-app guidance (PASS marker shape, `_race_signals` helper, mainnet gate semantics, button accessible_name pattern) is now CANONICALLY LOCATED in REVIEW.md per A-17. Story 5-5's `docs/install.md` + `docs/usage.md` should reference REVIEW.md rather than re-deriving the patterns.
