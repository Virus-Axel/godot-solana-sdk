<!--
Quality Signal: Inlined 9 sections (AC-1..AC-7 verbatim from plan.md:438-444; AC-FR-11 / AC-D-3 / AC-D-7 / AC-D-9 / AC-D-10 / AC-NFR-SEC-12 verbatim from spec.md:135/154/158/160/161/190; arch §3.2 signal-payload list cited by line range — NOT inlined verbatim, too large; arch §3.3 13-code error-taxonomy YAML manifest cited by line range; arch §2.2 Container View 13-row table cited as the canonical source for naming-map.md / api-reference.md component column; clientlib-ktx version 2.0.3 + compileSdk 34 + targetSdk 34 + minSdk 23 + compatibility_minimum "4.3" pulled from build.gradle.kts:35/46/47/225 + godot-solana-sdk.gdextension:4 — these are the AC-D-7 compatibility-matrix.md authoritative values; REVIEW.md ≤10-min reviewer flow + 17-row API surface table cross-referenced via Ref pointer (rule 4: discoverable, do not duplicate); divide-of-labor with 5-6 (5-5 ships data sources + scripts; 5-6 wires CI gates); existing isigner.md 175-line content audit — substantive guide already present, only AC-2 frontmatter retrofit needed). Refs 12 (docs/architecture.md §3.2:218-298 signal payload schemas + §3.3:316-409 error-codes.yaml manifest + §2.2:71-85 Container View 13-row table + line 1147 .gdextension drift closed by 5-4 + §10.4 R8 keep rules + DD-23 watchdog + DD-33 codegen-drift CI; docs/spec.md:135 AC-FR-11 + :154 AC-D-3 + :158 AC-D-7 + :160 AC-D-9 + :161 AC-D-10 + :174 AC-NFR-7 (MIT byte-diff) + :190 AC-NFR-SEC-12 + :213 FR-15 traceability + :104 security-auditor 12-concern list verbatim; docs/amendments.md A-12 1-param mwa_cancelled_lifecycle Dictionary contract + A-14 mwa_error 9-key payload canonical (note: spec says 10-key but actual signal definition at MWA.gd:34 is 9-key per A-14 final shape) + A-17 architectural relocation establishing REVIEW.md as canonical for example-app guidance; docs/plan.md:438-444 Story 5-5 verbatim + :463 Story 5-6 AC-3 verify_parity weekly cron + :461 Story 5-6 AC-1 scons addon + :466 Story 5-6 AC-6 grep bans + :490 FR-15 coverage map + :502 NFR-4 parity strategy; docs/stories/5-4.md DD-5-4-1/5 + REVIEW.md as canonical home for example-app design guidance per A-17; docs/stories/3-3.md DD-3-3-D one-shot pending_submission_found + DD-3-3-E 6-key payload + at-least-once semantics; docs/stories/4-2.md DD-4-2-* + mwa_cancelled_lifecycle 3-key payload {request_id, source_method, reason}; docs/stories/2-1.md SecureTokenStore + EncryptedSharedPreferences cluster-bound cache key (cluster, chain_id, wallet_package, identity_uri); REVIEW.md (208 lines) — canonical home for: PASS marker shape, _race_signals helper, mainnet gate semantics, Button.accessible_name pattern, AC-1 button list, state-transition log shape, 17-row API surface table — docs/install.md + docs/usage.md + docs/api-reference.md + docs/accessibility.md MUST cross-reference, NOT re-derive; addons/SolanaSDK/mwa/MWA.gd:27-47 (12 signals) + :133-418 (14 methods) — single source of truth for 13 docs that describe the API; android/plugin/build.gradle.kts:35,46,47,225 (compileSdk=34, minSdk=23, targetSdk=34, clientlib-ktx=2.0.3) — authoritative for compatibility-matrix.md; godot-solana-sdk.gdextension:4 (compatibility_minimum=4.3, post-DD-5-4-1) — authoritative for compat matrix Godot row; docs/migrations/isigner.md (175 lines, EXISTS, substantive — AC-2 frontmatter retrofit only, no content rewrite per disk audit). Omitted: full error-codes table inlined (cite arch §3.3:316-409 — 13 codes with kotlin_enum + cpp_enum + recoverable + retry_hint + default_user_message — too large to dup; docs/error-codes.md DERIVES from this manifest), full signal-payload schemas (cite arch §3.2:218-298 — docs/api-reference.md + docs/signals-101.md DERIVE), full RN method list with versions (the 7 RN methods are derived from `@solana-mobile/mobile-wallet-adapter-protocol` clientlib-ktx 2.0.3 — implementer constructs the YAML from the 7 op signals + clientlib-ktx Kotlin call sites; not pre-listed here because cataloging RN method signatures is itself a code-reading task that implementer owns), markdown lint config (project doesn't have one — implementer uses grep for structural assertions per orchestrator's tooling note), full LICENSE upstream text byte-diff procedure (T7 implements the script — cite spec.md:174 AC-NFR-7 for the contract; the upstream LICENSE is fetched at T-time, not pre-pinned to avoid stale text in this story file), curl/python YAML parser idioms (standard tooling), docs/dev/ existing dir listing (legacy maintainer notes, not in 5-5 scope). Closed exclusions per Inline Decision List rules 3-5: import order (discoverable from existing docs), markdown table syntax (universal pattern), git-add granularity (Code Standards §1).

Sizing audit conclusion: M is on the line. Deliverable surface is large (13 docs + LICENSE update + verify_docs.yml extension + CODEOWNERS + rn-parity YAML + 3 helper scripts), BUT each individual deliverable is small (most docs are 50-150 lines of straightforward content; the LICENSE byte-diff is mechanical; the YAML schema is 7 rows). Comparable to 5-3 (S, 382 lines but 5 LOCKED DDs across 4 prior systems) — 5-5 has 5 LOCKED DDs and similar story-file density, so M is acceptable IF the implementer uses the existing artifacts (REVIEW.md, MWA.gd, build.gradle.kts, arch §3.2/§3.3) as source-of-truth and DOES NOT re-derive content already canonicalized elsewhere. Recommendation: keep M; track scope creep in close-out — if any task expands beyond ~2 hr, file an amendment proposing L bump. Story-file target ~300-400 lines (matches 5-3's 382-line density profile). 5 LOCKED design decisions covering: divide-of-labor with 5-6, REVIEW.md as canonical example-app guidance (no duplication), isigner.md AC-2 frontmatter retrofit (preserve content), rn-parity YAML schema, LICENSE byte-diff handling-mode (manual fetch + inline + helper script ships in 5-5; PR-blocking CI gate ships in 5-6).

Live state confirmed at story-creation 2026-04-29:
- post-5-4 Gate 5 PASS (Wave 7 4/6 complete: 5-1, 5-2, 5-3, 5-4 done; 5-5 in-progress; 5-6 backlog) per sprint-status.yaml:194-216.
- baseline 277 GREEN (Kotlin + pytest combined; lastRefreshedAt 2026-04-29T15:30Z post-5-4 close-out per state.json:988-998).
- pre-tag pre-mwa-android-5-5 already at HEAD `39ff0d1f` per sprint-status.yaml:216 + state.json bookkeeping.
- AC-1 disk-state audit:
  - docs/migrations/isigner.md EXISTS (175 lines, substantive v1.0→v1.1→v1.2 ISigner migration guide — DD-5-5-3 LOCKS preserve+frontmatter-retrofit, NOT replace).
  - LICENSE EXISTS at repo root (22 lines, MIT, "Copyright (c) 2023 Axel" — needs AC-5 byte-diff verification against upstream solana-mobile/mobile-wallet-adapter LICENSE; if differs beyond the copyright line, T-LICENSE rewrites the body).
  - .github/workflows/verify_docs.yml EXISTS (19 lines; doxygen-only today; T-LINK-CHECK extends with markdown link-check job).
  - The other 12 docs do NOT exist (install.md, usage.md, api-reference.md, error-codes.md, compatibility-matrix.md, naming-map.md, rn-parity.md, api-stability.md, safety.md, triage.md, signals-101.md, accessibility.md) — verified via `ls /Users/valentinkrumov/Documents/godot-sdk/docs/*.md` at story-creation.
  - CODEOWNERS does NOT exist at repo root NOR at `.github/CODEOWNERS` — verified via `ls`. T-CODEOWNERS creates it.
- REVIEW.md EXISTS (208 lines, post-5-4 T1 deliverable at `0e10a495`; A-17 canonical home for example-app guidance — Story 5-5 DD-5-5-2 LOCKS cross-reference, never duplicate).
- Wave 7 CR cluster: CR-5-4-A (MWA.clear_local_cache facade gap — LOW), CR-5-4-B (Button.accessible_name version verification — LOW), CR-5-4-C (REVIEW.md release URL placeholder — LOW), CR-5-4-D (instrumented_emulator_fakewallet.yml redesign — MEDIUM, Story 5-6), CR-5-4-E (external repo CI verification gap — HIGH, user-owned), CR-5-4-F (release-on-tag CI MANDATED — Story 5-6), CR-5-4-G (pre-existing C++ compile blocker — HIGH, Story 5-6 to confront). 5-5 inherits NONE for direct closure; 5-5 closes ZERO concerns.
- API surface authoritative source: addons/SolanaSDK/mwa/MWA.gd lines 27-47 (12 signals) + 133-418 (14 methods: 7 ops + 1 forget_all + 1 is_supported + 5 state getters + 1 utility get_suggested_wallet_install_links). Verified at story-creation.
- Compat matrix authoritative values (verified at story-creation):
  - Godot: compatibility_minimum = "4.3" (godot-solana-sdk.gdextension:4 post-DD-5-4-1 fix at dd05abc5)
  - clientlib-ktx: 2.0.3 (build.gradle.kts:225)
  - compileSdk: 34, minSdk: 23, targetSdk: 34 (build.gradle.kts:35/46/47)
  - godot-cpp: tracked via .gitmodules submodule (SHA at story-creation: read at T2 time)
-->
---
id: "5-5"
slug: documentation-bundle
title: "Documentation Bundle (install · usage · API reference · error codes · compatibility matrix · naming map · RN parity · API stability · ISigner migration · safety · triage · signals 101 · accessibility) + LICENSE byte-diff + CODEOWNERS + verify_docs.yml link-check extension"
epic: 5
wave: 7
size: M
status: ready
dependencies: ["2-1", "2-2", "2-3", "3-1", "3-2", "3-3", "4-1", "4-2", "4-3", "5-2", "5-3"]
baselineTag: pre-mwa-android-5-5
created: 2026-04-29
---

# Story 5-5: Documentation Bundle

## User Story
*(verbatim from `docs/plan.md:429` — do not rephrase)*

As a Godot developer adopting the SDK, I want comprehensive docs covering install, usage, API reference, error codes, compatibility matrix, naming map, RN parity, API stability, ISigner migration, safety guidance, triage runbook, "Signals 101", and accessibility, so that I can ship a Solana-enabled game without reading source code.

## Context

- **Documentation-bundle story; ZERO production code surface.** No Kotlin, C++, GDScript, JNIEXPORT, or signal/payload changes in this story. The 13 docs DESCRIBE the production surface that landed in Stories 1-1 through 5-4; they do not extend it. Verification chain is structural greps + link checks + byte-diff for LICENSE — NO `./gradlew test` / pytest / androidTest impact. Baseline 277 GREEN must be preserved by the inertia of "no source surface touched" — any deviation is a regression.
- **REVIEW.md is the canonical home for example-app design guidance per A-17.** Story 5-4 T1 landed REVIEW.md (208 lines) at repo root with: PASS marker shape, `_race_signals` helper signature + location, mainnet gate per-launch semantics, `Button.accessible_name` `⚠ VERSION NOT VERIFIED` pattern, AC-1 9-button list with accessible-name strings, state-transition log shape (`[<ISO timestamp>] <method>: <state>` with 4 substates), and a 17-row API surface table. **DD-5-5-2 LOCKS:** `docs/install.md`, `docs/usage.md`, `docs/api-reference.md`, `docs/accessibility.md`, and `docs/safety.md` MUST cross-reference REVIEW.md for those patterns rather than re-deriving — duplication is a maintenance hazard (drift surface) and a Rule-2 deviation if it slips through review. Each doc cites REVIEW.md by section heading + repo-root path.
- **`docs/migrations/isigner.md` already exists with substantive content (175 lines).** Verified at story-creation 2026-04-29: the file is the v1.0 → v1.1 → v1.2 ISigner migration guide authored in Story 1-3, complete with deprecation timeline table, 7-row FAQ, before/after code examples for Keypair / WalletAdapter / MobileWalletAdapter, and a §5 "Reference" section pointing at architecture, amendments, and concerns. **DD-5-5-3 LOCKS:** preserve content; only retrofit the AC-2 `Owner:` + `Drift-check:` frontmatter at the top (insert before the existing `# Migrating to ISigner ...` heading). Do NOT rewrite. Do NOT consolidate. The §5 "Reference" tracked-concerns list (CR-14, CR-15, CR-16, CR-17) is preserved verbatim — those concerns are pre-MWA-LaiM and outside this story's closure scope.
- **`LICENSE` at repo root EXISTS but predates MWA work (22 lines, "Copyright (c) 2023 Axel").** AC-5 byte-diff says "differ only on copyright-holder line (byte-identical otherwise)." If the existing LICENSE body matches upstream `solana-mobile/mobile-wallet-adapter` MIT text byte-for-byte, T-LICENSE only updates the copyright-holder line (or leaves it — depends on T-time policy decision: keep "Axel" if pre-existing copyright is the project's first author, OR add "Copyright (c) 2024 LimeChain Tech / godot-solana-sdk contributors" depending on user direction at T-time). If the body DIVERGES from upstream, T-LICENSE rewrites the body to upstream's exact text and only adjusts the copyright-holder line. **DD-5-5-5 LOCKS** the procedure: T-LICENSE fetches upstream verbatim at T-time, runs byte-diff against the local LICENSE excluding the copyright line, and either (a) confirms match (no body rewrite needed) or (b) rewrites body to upstream verbatim. The byte-diff helper script (`tools/check_license_diff.sh` or equivalent) ships in 5-5 as a callable tool; the PR-blocking CI gate that invokes it lands in Story 5-6 per DD-5-5-1.
- **`.github/workflows/verify_docs.yml` EXISTS but only runs doxygen today (19 lines).** AC-4 EXTENDS — does NOT replace — the workflow with a markdown link-check job. The doxygen step is preserved; a second job (`link-check`) is added that runs against all `docs/*.md` files (and REVIEW.md). The link-check tool selection is implementer's choice (recommend `lychee` or `markdown-link-check` — both are mature; pick whichever has stable GitHub Actions wrapper at T-time). **DD-5-5-1 LOCKS** the divide-of-labor with Story 5-6: 5-5 SHIPS the workflow extension (PR-blocking on this repo immediately); 5-6 may further extend with deeper integration (e.g., re-running link-check on a weekly cron to catch external link-rot).
- **`docs/rn-parity.md` machine-readable YAML feeds Story 5-6 AC-3's weekly cron.** Story 5-6 AC-3 (plan.md:463) mandates `verify_parity.yml` runs weekly Sunday 03:00 UTC, diffing the pinned RN SDK npm package against `docs/rn-parity.md` method list. **DD-5-5-4 LOCKS** the YAML schema: a fenced ```yaml block with key `methods:` containing exactly 7 entries (one per op story), each entry a dict with keys `rn_method` (string), `rn_version` (string — the pinned `@solana-mobile/mobile-wallet-adapter-protocol` clientlib-ktx version, currently `2.0.3` per build.gradle.kts:225), `gdscript_method` (string — e.g., `MWA.connect`), `kotlin_method` (string — e.g., `MwaAndroidPlugin.mwaConnect`), `cpp_method` (string — e.g., `MobileWalletAdapter::mwa_connect`), `signal_names` (array of strings — completion + error signal names). 5-5 SHIPS the YAML + a one-shot validator script (`tools/validate_rn_parity.py` — parses the YAML, asserts schema, exits non-zero on drift); 5-6 wires the weekly cron that consumes the YAML.
- **CODEOWNERS does not exist.** AC-2 mandates a `docs/` entry. T-CODEOWNERS creates `CODEOWNERS` at repo root (GitHub recognizes this path; alternatives `.github/CODEOWNERS` and `docs/CODEOWNERS` also work — repo-root is the most discoverable). One `docs/` line + one wildcard fallback line is sufficient; the actual owner handle is set at T-time per user direction (likely `@ValentinVPK` based on existing branch + git config).
- **AC-7's safety.md spans 5 specific topics (security-auditor 12-concern list at spec.md:104).** The 5 mandated subsections are: (a) FLAG_SECURE recommendation, (b) identity-metadata attacker-controllability (`identityName`/`iconUri` per AC-NFR-SEC-12), (c) sign_and_send is at-least-once semantics (per Story 3-3 DD-3-3-A + arch:854 quote: "sign-and-send is at-least-once from the caller's perspective; treat breadcrumbs as authoritative for post-crash recovery"), (d) pending-submission recovery (the `pending_submission_found` one-shot signal per Story 3-3 DD-3-3-D), (e) secure logging practices (SecretString wrapper per AC-NFR-SEC-1; SdkLog stripped by R8 per AC-NFR-SEC-6; CI grep bans `Log.*` of `authToken`/`publicKey`/`SecretString.value` per Story 5-6 AC-6). **safety.md must explicitly cover all 5** — AC-7 enforces it; the must_have grep below verifies each substring presence.
- **Wave 7 sequencing — 5-5 is the penultimate story.** After 5-5: 5-6 is the final story (CI/Build Integration). 5-6 absorbs CR-5-4-D (instrumented_emulator_fakewallet.yml redesign), CR-5-4-F MANDATED (release-on-tag CI), and CR-5-4-G (pre-existing C++ compile blocker). 5-5 must NOT scope-creep into 5-6's CI work — specifically: `verify_parity.yml` cron schedule wiring, `byte_diff_license.yml` PR-blocking gate wiring (the LICENSE byte-diff helper script ships in 5-5, but the workflow that fails PR on drift ships in 5-6). Per DD-5-5-1, this divide is enforced via Out-of-Scope below.

## Acceptance Criteria
*(verbatim from `docs/plan.md:438-444` — do not rephrase)*

- **AC-1 (docs complete):** *Given* the doc tree, *when* listing, *then* the following files exist with non-empty content: `docs/install.md`, `docs/usage.md`, `docs/api-reference.md`, `docs/error-codes.md`, `docs/compatibility-matrix.md`, `docs/naming-map.md`, `docs/rn-parity.md`, `docs/api-stability.md`, `docs/migrations/isigner.md`, `docs/safety.md`, `docs/triage.md`, `docs/signals-101.md`, `docs/accessibility.md`.
  - **Evidence path:** `for f in install usage api-reference error-codes compatibility-matrix naming-map rn-parity api-stability safety triage signals-101 accessibility; do test -s "docs/$f.md" || echo "MISSING: docs/$f.md"; done; test -s "docs/migrations/isigner.md" || echo "MISSING: docs/migrations/isigner.md"` — must produce zero `MISSING:` lines AND each file's `wc -l` ≥ 30 (rough lower bound for "non-empty content"; rn-parity.md, naming-map.md may be shorter — 20-line floor for those two).
- **AC-2 (ownership headers):** *Given* each new doc, *when* its first 10 lines are inspected, *then* they contain `Owner:` and `Drift-check:` frontmatter identifying the maintainer and which CI job enforces it; CODEOWNERS entry for `docs/` exists.
  - **Evidence path:** for each of the 13 doc files, `head -10 docs/<f>.md | grep -cE '^Owner:|^Drift-check:'` returns 2 (one Owner line + one Drift-check line); `grep -cE '^/?docs/' CODEOWNERS` returns ≥1.
- **AC-3 (7/7 API reference):** *Given* `docs/api-reference.md`, *when* a Godot dev reads it, *then* each of 7 MWA methods + state getters + utility methods has: signature, parameters, return, signals emitted (success + all error codes), copy-pasteable GDScript snippet.
  - **Evidence path:** `grep -cE '^### MWA\.(connect|disconnect|reauthorize|deauthorize|sign_messages|sign_transactions|sign_and_send)' docs/api-reference.md` returns ≥7 (the 7 ops); `grep -cE '^### MWA\.(forget_all|is_supported|is_connected|get_public_key|get_cluster|get_wallet_label|get_diagnostics|get_device_posture|get_suggested_wallet_install_links|get_auth_token_fingerprint)' docs/api-reference.md` returns ≥10 (state getters + utility — 10 of them per MWA.gd:334-418); `grep -cE '^```gdscript' docs/api-reference.md` returns ≥17 (one snippet per method).
- **AC-4 (link check):** *Given* CI `verify_docs.yml` is extended, *when* it runs on every PR, *then* any broken markdown link to a local path or external URL fails the build.
  - **Evidence path:** `.github/workflows/verify_docs.yml` has TWO jobs (existing `verify` doxygen + NEW `link-check`); `grep -cE 'markdown-link-check|lychee' .github/workflows/verify_docs.yml` returns ≥1; the link-check job runs `on: pull_request` (preserved from existing trigger).
- **AC-5 (LICENSE byte-diff):** *Given* repo root `LICENSE`, *when* CI compares against upstream `solana-mobile/mobile-wallet-adapter` `LICENSE` (MIT text), *then* they differ only on the copyright-holder line (byte-identical otherwise).
  - **Evidence path:** `tools/check_license_diff.sh` (NEW helper script in 5-5) runs `diff <(curl -s https://raw.githubusercontent.com/solana-mobile/mobile-wallet-adapter/main/LICENSE | grep -v '^Copyright') <(grep -v '^Copyright' LICENSE)` and exits 0 on byte-identical body; T-LICENSE invokes this locally, captures the result, and either confirms (LICENSE already conformant) or rewrites the body. The PR-blocking CI gate that invokes the script ships in Story 5-6 per DD-5-5-1.
- **AC-6 (rn-parity machine-readable):** *Given* `docs/rn-parity.md`, *when* a parser reads a YAML table of 7 rows, *then* each row has `rn_method`, `rn_version`, `gdscript_method`, `kotlin_method`, `cpp_method`, `signal_names`; consumed by the weekly `verify_parity.yml` cron job.
  - **Evidence path:** `tools/validate_rn_parity.py` (NEW) parses the YAML block from rn-parity.md, asserts `len(methods) == 7` AND each entry has all 6 keys AND each value type matches schema. Exit 0 on success, non-zero on drift. The weekly cron that invokes this script lands in Story 5-6 AC-3.
- **AC-7 (safety doc):** *Given* `docs/safety.md`, *when* a dev reads it, *then* it covers: `FLAG_SECURE` recommendation, identity-metadata attacker-controllability, sign_and_send is at-least-once semantics, pending-submission recovery, and secure logging practices.
  - **Evidence path:** `grep -cE 'FLAG_SECURE' docs/safety.md` ≥1; `grep -cE 'identityName.*attacker|attacker.*identityName|attacker-controllab' docs/safety.md` ≥1; `grep -cE 'at-least-once' docs/safety.md` ≥1; `grep -cE 'pending_submission_found|pending-submission' docs/safety.md` ≥1; `grep -cE 'SecretString|secure logging|R8.*strip|SdkLog' docs/safety.md` ≥1. All 5 must hit.

### AC → Task Mapping
| AC | Covered By Tasks |
|----|------------------|
| AC-1 | T1 (foundation cluster: install/usage/api-reference/error-codes), T2 (compat cluster: compatibility-matrix/naming-map/rn-parity/api-stability), T3 (safety cluster: safety/triage/signals-101/accessibility), T4 (isigner.md frontmatter retrofit) |
| AC-2 | T1+T2+T3+T4 (each task adds frontmatter to its doc cluster), T5 (CODEOWNERS) |
| AC-3 | T1 (api-reference.md content) |
| AC-4 | T6 (verify_docs.yml link-check extension) |
| AC-5 | T7 (LICENSE byte-diff + helper script) |
| AC-6 | T2 (rn-parity.md YAML + validator script) |
| AC-7 | T3 (safety.md 5-topic coverage) |

## Architecture Guardrails

5 LOCKED design decisions covering: divide-of-labor with Story 5-6, REVIEW.md as canonical example-app guidance (no duplication), isigner.md preservation (frontmatter retrofit only), rn-parity YAML schema, LICENSE byte-diff handling-mode.

- **DD-5-5-1 (LOCKED) — Divide-of-labor with Story 5-6: 5-5 ships data sources + helper scripts; 5-6 wires PR-blocking CI gates.** Three artifacts have a 5-5 / 5-6 split:
  1. **`verify_docs.yml` link-check (AC-4):** 5-5 EXTENDS the existing workflow with the link-check job (PR-blocking on this repo immediately at T6 commit time). 5-6 may further enhance (weekly cron for external link-rot, retry policy, allow-list for known-flaky external URLs). The PR-blocking gate lives in 5-5; 5-6 enhances.
  2. **`verify_parity.yml` weekly cron (AC-6 → 5-6 AC-3):** 5-5 ships `docs/rn-parity.md` + `tools/validate_rn_parity.py`. 5-6 wires the weekly cron workflow that invokes the validator. 5-5 does NOT create `verify_parity.yml`. Justification: cron scheduling (Sunday 03:00 UTC per plan.md:463) + issue-opening logic (`gh issue create --label parity-drift` on new method found) + npm-package-fetch-and-diff is non-trivial CI work that belongs in 5-6's CI cluster. Splitting it from 5-5's data-source authoring keeps each story coherent.
  3. **LICENSE byte-diff CI (AC-5 → 5-6):** 5-5 ships `tools/check_license_diff.sh` + ensures the existing LICENSE conforms. 5-6 wires the PR-blocking workflow (likely `byte_diff_license.yml` or extends `verify_docs.yml`'s job graph) that invokes the script on every PR. **Rationale:** consistency with verify_parity split + the byte-diff CI is mechanical-but-CI-coupled (caching the upstream URL, handling fetch failures, etc.) — belongs in 5-6.
  - **Rejected alternative (ship all CI in 5-5):** would balloon 5-5 to L size (3 new workflows + tests) AND fragments 5-6's CI cluster (5-6 would inherit half-built workflows). Cleaner to keep CI-as-a-cluster in 5-6.

- **DD-5-5-2 (LOCKED) — REVIEW.md is canonical home for example-app design guidance; 5-5 docs cross-reference, never duplicate.** Five 5-5 docs touch content REVIEW.md already canonicalizes:
  - **`docs/install.md`** — REVIEW.md is the reviewer-quick-path (≤10-min flow); install.md is the developer reference (full toolchain setup, NDK install, Gradle config, addon download paths). install.md's "Quick path for grant reviewers" subsection links to REVIEW.md rather than duplicating the ≤10-min steps. The two docs are COMPLEMENTARY: REVIEW.md = reviewer ≤10-min; install.md = developer in-depth.
  - **`docs/usage.md`** — references REVIEW.md's "Design guidance for the example-repo implementer" section for: `_race_signals` helper (`<example-repo>/scripts/await_helpers.gd` per REVIEW.md's design guidance section), PASS marker shape, mainnet gate semantics, button accessible-name pattern. usage.md describes how a Godot game OUTSIDE the example app should adopt the patterns; REVIEW.md describes how the EXAMPLE app implements them.
  - **`docs/api-reference.md`** — extends REVIEW.md's 17-row API surface table with parameter-level detail + GDScript snippets per AC-3. Cite REVIEW.md as the row-level source: "The summary table in REVIEW.md lists all 17 symbols at a glance; this doc provides per-symbol detail."
  - **`docs/accessibility.md`** — references REVIEW.md's `Button.accessible_name` `⚠ VERSION NOT VERIFIED` marker (CR-5-4-B) + WCAG AA contrast guidance. accessibility.md adds: keyboard navigation, focus management, ARIA-equivalent properties on Godot Control nodes, screen-reader compat notes for TalkBack (Android) + VoiceOver (iOS — out of scope but mentioned).
  - **`docs/safety.md`** — references REVIEW.md's mainnet-gate semantics (per-launch confirmation, `identity_uri` placeholder); safety.md does NOT re-derive the gate; it documents WHY the gate matters (AC-NFR-SEC-12 phishing concern).
  - **Rejected alternative (duplicate the patterns into each doc):** drift surface — if REVIEW.md updates a pattern (e.g., `Button.accessible_name` resolves to `accessibility_name`), all 5 docs would need synchronized updates. Cross-reference avoids the drift surface entirely.

- **DD-5-5-3 (LOCKED) — `docs/migrations/isigner.md` PRESERVED; only AC-2 frontmatter retrofit.** The file is 175 lines of substantive v1.0 → v1.1 → v1.2 ISigner migration content authored in Story 1-3 (deprecation timeline table, 7-row FAQ, before/after code examples for Keypair / WalletAdapter / MobileWalletAdapter, §5 "Reference" with cross-links). Story 5-5 inserts ONLY the AC-2 frontmatter (3-line block: `Owner:` + `Drift-check:` + a blank line, OR a YAML frontmatter block with `---` delimiters depending on T-time markdown-render preference) at the top, BEFORE the existing `# Migrating to ISigner ...` heading. Do NOT rewrite content. Do NOT consolidate. The §5 "Reference" tracked-concerns list (CR-14, CR-15, CR-16, CR-17) is preserved verbatim — those concerns are pre-MWA-LaiM and outside this story's closure scope.
  - **Pre-edit grep:** `head -1 docs/migrations/isigner.md` returns `# Migrating to \`ISigner\` (v1.0 → v1.1 → v1.2)`. Post-edit: `head -3 docs/migrations/isigner.md` returns the new 3-line frontmatter (or `---` + Owner + Drift-check + `---` if YAML form chosen).
  - **Rejected alternative (replace with stub):** loses 175 lines of authoritative migration content that consumers grep for the deprecation warning string (the warning text in `src/transaction/transaction.cpp` references `docs/migrations/isigner.md` verbatim per Q3 of the existing FAQ — see line 97 of the existing file). Stubbing breaks that contract.

- **DD-5-5-4 (LOCKED) — `docs/rn-parity.md` YAML schema: 7 entries × 6 keys, fenced as ` ```yaml ` block.** Schema:
  ```yaml
  # docs/rn-parity.md (machine-readable section)
  schema_version: 1
  pinned_clientlib_ktx_version: "2.0.3"  # from android/plugin/build.gradle.kts:225
  methods:
    - rn_method: "<RN method name e.g. authorize>"
      rn_version: "2.0.3"
      gdscript_method: "MWA.connect"
      kotlin_method: "MwaAndroidPlugin.mwaConnect"
      cpp_method: "MobileWalletAdapter::mwa_connect"
      signal_names: ["connect_completed", "mwa_error"]
    # ... 6 more entries (disconnect, reauthorize, deauthorize, sign_messages, sign_transactions, sign_and_send)
  ```
  - **Why YAML in markdown (not standalone .yaml file):** AC-6 says "`docs/rn-parity.md`" is the consumer surface; the YAML must be embedded in the markdown so readers see it AND the cron can extract it. `tools/validate_rn_parity.py` strips the markdown frame (extracts content between ` ```yaml ` and ` ``` ` fences) then runs `yaml.safe_load`. Standard pattern.
  - **The 7 op rows:** correspond 1:1 with the 7 op stories (Stories 2-1 connect, 2-2 reauthorize, 2-3 disconnect, 3-1 sign_messages, 3-2 sign_transactions, 3-3 sign_and_send, 4-1 deauthorize). NOTE: forget_all (Story 4-2) is NOT one of the 7 RN-parity methods — RN SDK has no forget_all equivalent; forget_all is a godot-mwa-native GDPR/CCPA addition per AC-NFR-SEC-11. The implementer MUST verify this at T2 by reading `@solana-mobile/mobile-wallet-adapter-protocol` clientlib-ktx 2.0.3 method list and confirming forget_all is absent — if present, file an amendment proposing the YAML be 8 entries.
  - **Rejected alternative (CSV or markdown table):** harder to parse in CI (markdown table parsers are brittle); YAML has stable libraries. Rejected alternative (separate `.yaml` file): hides the schema from human readers of rn-parity.md, splits the content surface unnecessarily.

- **DD-5-5-5 (LOCKED) — `LICENSE` byte-diff procedure: fetch upstream verbatim, byte-diff body excluding copyright line, conform if drift detected.** AC-5 contract: "differ only on the copyright-holder line (byte-identical otherwise)." The existing LICENSE at repo root is "Copyright (c) 2023 Axel" + standard MIT body. T-LICENSE procedure:
  1. **Fetch upstream:** `curl -s https://raw.githubusercontent.com/solana-mobile/mobile-wallet-adapter/main/LICENSE > /tmp/upstream-license.txt`.
  2. **Byte-diff bodies (excluding copyright lines):** `diff <(grep -v '^Copyright' /tmp/upstream-license.txt) <(grep -v '^Copyright' LICENSE)`. If empty → conform (no body changes needed). If non-empty → upstream MIT body has been adjusted (rare; upstream uses standard MIT verbatim) — implementer rewrites the local LICENSE body to upstream verbatim.
  3. **Copyright-holder line policy:** keep the existing "Copyright (c) 2023 Axel" line if it represents the project's first author and user direction at T-time confirms (likely — this is the godot-solana-sdk's pre-MWA copyright). OR add a second "Copyright (c) 2024-2026 LimeChain Tech / godot-solana-sdk MWA contributors" line if user direction at T-time prefers attribution to the MWA work. **AC-5 doesn't mandate the holder name** — only that the body matches upstream byte-for-byte. **T-LICENSE asks user at T-time** if any holder-line edit is needed; the default is to leave unchanged.
  4. **Helper script `tools/check_license_diff.sh`:** ships in 5-5; encapsulates steps 1-2; exits 0 on conform, non-zero on drift; prints the diff. The PR-blocking CI workflow that invokes it ships in Story 5-6 per DD-5-5-1.
  - **Rejected alternative (auto-fetch on every CI run, no committed LICENSE diff):** fragile — if upstream URL goes 404 (or upstream changes the path), CI breaks. Better: commit a verified LICENSE in 5-5; CI re-verifies on every PR via fetch+diff but the source-of-truth is the committed file.
  - **Rejected alternative (vendor upstream LICENSE wholesale into a `THIRD_PARTY_LICENSES.md`):** the AC contract is about the repo root LICENSE, not a third-party-licenses doc. Out of scope.

## Verified Interfaces

This story produces 13 markdown documents + 2 helper scripts + 1 workflow extension + 1 CODEOWNERS file. The documents DESCRIBE the production surface verified at story-creation. NO new function/method/class/signal interfaces are introduced. The "interface" being verified is the EXISTING surface that the docs describe.

| Interface (described surface) | Authoritative source | Status |
|-------------------------------|----------------------|--------|
| 12 signals on `MWA.gd` (`connect_completed`, `reauthorize_completed`, `disconnect_completed`, `deauthorize_completed`, `sign_messages_completed`, `sign_transactions_completed`, `sign_and_send_completed`, `mwa_error`, `mwa_timeout`, `mwa_cancelled_lifecycle`, `reauth_required`, `pending_submission_found`) | `addons/SolanaSDK/mwa/MWA.gd:27-47` | ✓ VERIFIED |
| 14 methods on `MWA.gd` (7 ops: connect/disconnect/reauthorize/deauthorize/sign_messages/sign_transactions/sign_and_send + forget_all + is_supported + 5 state getters: is_connected/get_public_key/get_cluster/get_wallet_label/get_auth_token_fingerprint + get_diagnostics + get_device_posture + get_suggested_wallet_install_links) | `addons/SolanaSDK/mwa/MWA.gd:133-418` | ✓ VERIFIED — note 14 methods (not 7); api-reference.md AC-3 covers all of them |
| Signal payload schemas (12 dictionaries) | `docs/architecture.md` §3.2:218-298 | ✓ VERIFIED — api-reference.md + signals-101.md cite this section |
| Error-codes manifest (13 codes: USER_CANCELED, WALLET_REJECTED, NO_MWA_WALLET_INSTALLED, WALLET_UNINSTALLED, TOKEN_EXPIRED, RPC_FAILED, NETWORK_OFFLINE, TIMEOUT, NOT_CONNECTED, UNSUPPORTED_PLATFORM, PROTOCOL_ERROR, STORAGE_CORRUPT, REAUTH_REQUIRED) | `docs/architecture.md` §3.3:316-409 | ✓ VERIFIED — error-codes.md derives from this manifest |
| `mwa_error` 9-key payload `{request_id, code, user_message, developer_details, recoverable, retry_hint, layer, cause, source_method}` (per A-14 final shape; spec.md historical references to 10-key are pre-A-14) | `docs/architecture.md` §3.2:277-290 + `docs/amendments.md` A-14 | ✓ VERIFIED |
| `mwa_cancelled_lifecycle` 3-key payload `{request_id, source_method, reason}`; reason ∈ `{"forget_all_invoked", "activity_destroyed"}` | `docs/architecture.md` §3.2 + Story 5-3 DD-5-3-4 | ✓ VERIFIED |
| `pending_submission_found` 6-key payload `{request_id, op_type, started_at_ms, tx_count, tx_preview_hashes, recommendation}` ONE-SHOT per Story 3-3 DD-3-3-D | `addons/SolanaSDK/mwa/MWA.gd:47` + `docs/stories/3-3.md` DD-3-3-D/E | ✓ VERIFIED |
| `compatibility_minimum = "4.3"` (post-DD-5-4-1 fix at `dd05abc5`) | `godot-solana-sdk.gdextension:4` | ✓ VERIFIED at story-creation |
| `compileSdk = 34`, `targetSdk = 34`, `minSdk = 23` | `android/plugin/build.gradle.kts:35,46,47` | ✓ VERIFIED at story-creation |
| `mobile-wallet-adapter-clientlib-ktx:2.0.3` | `android/plugin/build.gradle.kts:225` | ✓ VERIFIED at story-creation |
| `godot-cpp` submodule (SHA per `.gitmodules` + `git submodule status godot-cpp` at T2 time) | `.gitmodules:1-3` + working tree | ⚠ READ AT T2 — submodule SHA must be captured in compatibility-matrix.md as part of the matrix; pre-edit `git submodule status godot-cpp` returns the commit SHA |
| Container View 13-row component table (A-M: MWA.gd / MobileWalletAdapter / MwaAndroidBridge / MwaAndroidBridgeJni / MwaAndroidPlugin / MwaClient / MwaClientImpl / SecureTokenStore / MwaSessionState / SdkLog / SecretString / ErrorTaxonomy / Example app) | `docs/architecture.md` §2.2:71-85 | ✓ VERIFIED — naming-map.md cites as the canonical component-name source |
| REVIEW.md 17-row API surface table + Design Guidance section | `/Users/valentinkrumov/Documents/godot-sdk/REVIEW.md:178-199` (API table) + `:135-173` (design guidance) | ✓ VERIFIED at story-creation |
| `docs/migrations/isigner.md` 175-line content (preserved by DD-5-5-3) | `/Users/valentinkrumov/Documents/godot-sdk/docs/migrations/isigner.md` | ✓ VERIFIED at story-creation; 175 lines confirmed |
| Existing `LICENSE` 22-line MIT (`Copyright (c) 2023 Axel`) | `/Users/valentinkrumov/Documents/godot-sdk/LICENSE` | ✓ VERIFIED at story-creation; T-LICENSE byte-diff against upstream at T-time |
| Existing `.github/workflows/verify_docs.yml` 19-line doxygen-only workflow | `/Users/valentinkrumov/Documents/godot-sdk/.github/workflows/verify_docs.yml` | ✓ VERIFIED at story-creation; T-LINK-CHECK extends |

## Tasks

- [ ] **T1 — Foundation cluster: `docs/install.md`, `docs/usage.md`, `docs/api-reference.md`, `docs/error-codes.md` + AC-2 frontmatter for each.**
  - **`docs/install.md` (~150 lines target):** developer install path. Sections: (1) prerequisites (Godot 4.3.x, Android SDK + NDK + emulator OR device, scons, Java 17 for Gradle), (2) install via tagged release (`gh release download v0.1.0 --pattern '*.zip' && unzip ...` — point at this repo's GitHub Releases per DD-5-4-5; until 5-6 ships release-on-tag CI per CR-5-4-F MANDATED, link to "build from source" subsection), (3) install via build-from-source (`scons addon` per Story 5-6 AC-1; note CR-5-4-G HIGH C++ compile blocker as a known issue), (4) project setup (autoload registration in project.godot — addon's `solana_sdk_plugin.gd` registers `MWA` autoload), (5) Quick path for grant reviewers — link to REVIEW.md (DD-5-5-2 cross-reference; do NOT duplicate the ≤10-min flow), (6) troubleshooting (compatibility_minimum mismatch on Godot 4.4 attempt; missing AAR symptoms; emulator architecture mismatches).
  - **`docs/usage.md` (~180 lines target):** usage patterns for game devs OUTSIDE the example app. Sections: (1) Hello, MWA — minimal connect-and-disconnect (5-line snippet); (2) The await + signal pattern (use REVIEW.md's `_race_signals` helper as the canonical pattern; cite REVIEW.md's design guidance section per DD-5-5-2 — usage.md describes the pattern, REVIEW.md hosts the reference impl); (3) Cluster selection (devnet / testnet / mainnet-beta — when to use each; cite AC-NFR-SEC-10 mainnet-gate guidance); (4) Identity metadata (`identity_uri` placeholder per spec.md:188; safety guidance link to safety.md); (5) Sign-and-send recovery (cite Story 3-3 DD-3-3-D one-shot pending_submission_found); (6) Cancel-in-flight (`MWA.forget_all()` per Story 4-2; note AC-A-2 lifecycle cancel via Story 5-3 observer); (7) Error handling (cite error-codes.md + signals-101.md).
  - **`docs/api-reference.md` (~400 lines target — the largest doc; AC-3 mandates 7/7 method coverage with snippets):** per-method reference. Use REVIEW.md's 17-row table as the table-of-contents; this doc's per-method sections add: signature (verbatim from MWA.gd line N), parameters (each parameter typed + described), return type, signals emitted on success, error codes possible (cite error-codes.md), copy-pasteable GDScript snippet (5-15 lines per method showing await-pattern usage). AC-3 enforces: 7 ops + state getters + utility methods. Mechanically: include ALL 14 methods from MWA.gd:133-418 (7 ops + forget_all + is_supported + 6 state getters = 14; the 17-row REVIEW.md table also includes 12 signals which api-reference.md covers in a "Signals" subsection citing arch §3.2 for full payload schemas).
  - **`docs/error-codes.md` (~120 lines target):** the 13-code error taxonomy. Source: arch §3.3:316-409 `error-codes.yaml` manifest. Format: one subsection per code with: enum name, recoverable boolean, retry_hint, default_user_message (verbatim from manifest), `source_method` values where this code can fire (cross-reference to op stories), recovery pattern (cite arch §5.3:632-640 6-row recovery table). AC-D-3 requires CI drift check ties this doc to the YAML manifest — note: drift CI lands in 5-6 (codegen_drift_check.yml already exists per `.github/workflows/codegen_drift_check.yml`).
  - **AC-2 frontmatter** at top of each doc (4 docs):
    ```
    Owner: <user-handle e.g. @ValentinVPK>
    Drift-check: verify_docs.yml
    ```
    (The `Drift-check:` value names which CI workflow enforces freshness — for content docs that's `verify_docs.yml` link-check; for error-codes.md it's `codegen_drift_check.yml` since the YAML manifest drives it.)
  - **Pre-edit grep:** `for f in install usage api-reference error-codes; do test -f "docs/$f.md" && echo "EXISTS: $f"; done` → all 4 should NOT exist (T1 creates them).
  - **Verification:** AC-1 evidence path subset (4 docs exist + ≥30 lines each); AC-2 frontmatter grep on each; AC-3 grep on api-reference.md (7 op headers + 10 utility headers + ≥17 ```gdscript blocks).
  - Maps to: AC-1 (4/13 docs), AC-2 (4 frontmatters), AC-3 (api-reference.md content)
  - Files: `docs/install.md` (new), `docs/usage.md` (new), `docs/api-reference.md` (new), `docs/error-codes.md` (new).
  - TDD: N/A (documentation; verification is structural greps + AC-3 method-coverage greps)

- [ ] **T2 — Compat cluster: `docs/compatibility-matrix.md`, `docs/naming-map.md`, `docs/rn-parity.md`, `docs/api-stability.md` + AC-2 frontmatter + `tools/validate_rn_parity.py`.**
  - **`docs/compatibility-matrix.md` (~80 lines target):** AC-D-7 surface. Single matrix table with rows: `Godot minor` (4.3.x; cite godot-solana-sdk.gdextension:4 `compatibility_minimum = "4.3"` per DD-5-4-1), `godot-cpp SHA` (read at T2 from `git submodule status godot-cpp`), `mobile-wallet-adapter-clientlib-ktx` (`2.0.3` per build.gradle.kts:225), `compileSdk` (`34`), `targetSdk` (`34`), `minSdk` (`23`), `Kotlin` (read at T2 from build.gradle.kts kotlinOptions or settings.gradle.kts toolchain), `JDK` (17 per scaffold), `Android Gradle Plugin` (read at T2 from `gradle/wrapper/gradle-wrapper.properties` + `android/build.gradle.kts` plugin block), `last verified` (`2026-04-29` at T2 commit time). Story 5-6 AC-8 wires CI drift check that fails build if `build.gradle.kts` or `.gdextension` drifts from these values.
  - **`docs/naming-map.md` (~100 lines target):** AC-D-9 single table mapping RN method → GDScript → C++ → Kotlin `@UsedByGodot` → signal names. 7 op rows (matches rn-parity.md's 7 entries). Cite arch §2.2:71-85 Container View for the canonical Component column (A=MWA.gd, B=MobileWalletAdapter, E=MwaAndroidPlugin). AC-D-9 requires CI parses APIs and fails on divergence — that CI lands in 5-6 (likely `naming_map_drift.yml` or extends `codegen_drift_check.yml`); 5-5 ships the source-of-truth doc.
  - **`docs/rn-parity.md` (~60 lines target — schema + 7 rows):** AC-D-3 + AC-6 surface. Markdown intro paragraph (1-2 sentences explaining "this doc is consumed by `verify_parity.yml` weekly cron per Story 5-6 AC-3") + the YAML block per DD-5-5-4 schema (7 entries × 6 keys, fenced as ` ```yaml ` block, top-level `pinned_clientlib_ktx_version: "2.0.3"`). The 7 RN method names: implementer constructs from the 7 op stories' Kotlin `@UsedByGodot` method names (Stories 2-1/2-2/2-3/3-1/3-2/3-3/4-1) cross-referenced with `@solana-mobile/mobile-wallet-adapter-protocol` clientlib-ktx 2.0.3 method list (likely: `authorize`, `reauthorize`, `deauthorize`, `signMessages`, `signTransactions`, `signAndSendTransactions`, plus one disconnect-equivalent — the implementer reads clientlib-ktx 2.0.3's `MobileWalletAdapterClient.kt` API at T2 time and confirms the 7-name list). VERIFY at T2: `forget_all` is NOT one of the 7 RN-parity methods (it's a godot-mwa-native GDPR/CCPA addition per AC-NFR-SEC-11; if RN clientlib-ktx 2.0.3 has an equivalent, file an amendment proposing 8 entries).
  - **`tools/validate_rn_parity.py` (~50 lines target):** stdlib-only Python script (use `pyyaml` if available else regex-extract). Steps: (1) read `docs/rn-parity.md`, (2) extract the ` ```yaml ` block content, (3) `yaml.safe_load` into a dict, (4) assert `schema_version == 1`, `pinned_clientlib_ktx_version` is a string, `len(methods) == 7`, each entry is a dict with all 6 keys (rn_method/rn_version/gdscript_method/kotlin_method/cpp_method/signal_names), (5) exit 0 on success, exit 1 on schema violation (print specific failure). The Story 5-6 AC-3 weekly cron invokes this script.
  - **`docs/api-stability.md` (~80 lines target):** AC-D-10 surface. Sections: (1) versioning policy (semver; v0.1.0 = first stable cut per Story 5-6 release-on-tag CI; pre-v0.1.0 = experimental — cite CR-5-4-F MANDATED), (2) deprecation ladder (mark → warn-one-minor → remove; cite arch §1166 + DD-25 LOCKED + the ISigner v1.0→v1.1→v1.2 example in `docs/migrations/isigner.md`), (3) `@since` XML tags (cite arch line ~1216 maintainer review #8 — `scripts/check_since_tags.py` lands in 5-6 AC-7), (4) signal payload additive-keys-only contract (cite AC-D-10 + arch §3.2:220 "All signals emit a single `Dictionary` argument (AC-D-10 — additive keys only)"), (5) what counts as a breaking change (signal payload key removal, signal removal, method removal, `request_id` format change, error code semantic change), (6) what does NOT count as breaking (new signals, new method args with defaults, new optional Dictionary keys, new error codes added to existing taxonomy).
  - **AC-2 frontmatter** at top of each of the 4 docs (`Owner:` + `Drift-check:`).
  - **Pre-edit grep:** all 4 docs should NOT exist; `tools/validate_rn_parity.py` should NOT exist.
  - **Verification:** AC-1 evidence path subset (4 docs exist + ≥30 lines for the longer 3, ≥20 for rn-parity); AC-6 — running `python tools/validate_rn_parity.py` exits 0; AC-2 frontmatter grep on each; manual: implementer reads clientlib-ktx 2.0.3 method list and confirms the 7 names.
  - Maps to: AC-1 (4/13 docs), AC-2 (4 frontmatters), AC-6 (rn-parity.md YAML + validator)
  - Files: `docs/compatibility-matrix.md` (new), `docs/naming-map.md` (new), `docs/rn-parity.md` (new), `docs/api-stability.md` (new), `tools/validate_rn_parity.py` (new).
  - TDD: N/A for the docs; partial for the script (a 1-2 case smoke unit test in `tools/test_validate_rn_parity.py` is OPTIONAL — implementer choice based on story budget; if the script is ≤50 lines of straight-line parsing+assertion, smoke-by-eye is acceptable per existing tools/ convention).

- [ ] **T3 — Safety cluster: `docs/safety.md`, `docs/triage.md`, `docs/signals-101.md`, `docs/accessibility.md` + AC-2 frontmatter.**
  - **`docs/safety.md` (~150 lines target — AC-7 mandates 5 specific topics):** Sections (in this order):
    1. **`FLAG_SECURE` recommendation** (AC-NFR-SEC-12) — when to add `getWindow().addFlags(LayoutParams.FLAG_SECURE)` to game Activities displaying public keys / transaction details / signing prompts; how to do it from a Godot game (note: Godot's main Activity in the AAR template does NOT default to FLAG_SECURE; game devs add it via custom Activity export template OR via a Godot Android plugin hook — point at Godot 4.3 docs).
    2. **Identity-metadata attacker-controllability** (AC-NFR-SEC-12) — `identityName` / `iconUri` / `identityUri` on `MWA.connect(identity, cluster, opts)` are presented to the wallet as part of the signing prompt; the WALLET'S signing prompt is the sole trust anchor (a hostile dApp cannot fake the wallet UI, but it CAN supply misleading identity metadata). Recommend: use a real `identity_uri` for production (NOT the `https://example.com/godot-mwa-sample` placeholder per AC-NFR-SEC-10 + REVIEW.md design guidance); never use HTTPS-vs-HTTP downgrade in identity_uri; teach users to verify the wallet prompt's domain field before signing.
    3. **`sign_and_send` is at-least-once semantics** (Story 3-3 DD-3-3-A + arch:854) — quote: "sign-and-send is at-least-once from the caller's perspective; treat breadcrumbs as authoritative for post-crash recovery." Explain why: SDK cannot autonomously confirm on-chain landing without an RPC endpoint config. Caller responsibility: query `getSignatureStatuses` via existing SolanaClient nodes after the success signal fires.
    4. **Pending-submission recovery** (Story 3-3 DD-3-3-D) — the `pending_submission_found` ONE-SHOT signal fires on next `connect`/`reauthorize` if a prior `sign_and_send` was interrupted by process death. 6-key payload `{request_id, op_type, started_at_ms, tx_count, tx_preview_hashes, recommendation}`. Caller pattern: subscribe via `MWA.pending_submission_found.connect(handler, CONNECT_ONE_SHOT)` in `_ready()`; query `getSignatureStatuses` for the listed `tx_preview_hashes`; resubmit only if all hashes are NOT found on-chain.
    5. **Secure logging practices** — SecretString wrapper (AC-NFR-SEC-1) — every token/pubkey field flows through `SecretString` whose `toString() == "<redacted>"`; never log `authToken` / `publicKey` / `SecretString.value` directly. R8-strip via `SdkLog.v/d` (AC-NFR-SEC-6) — release builds zero-out verbose logging via `-assumenosideeffects`. CI grep bans (Story 5-6 AC-6) — `Log.*` of `authToken` / `publicKey` / `SecretString.reveal` fails build; `SdkLog.v/d/i/w/e` with string interpolation `${...}` fails (must use lambda form).
    - **All 5 topics MUST hit AC-7 evidence path greps** (FLAG_SECURE + attacker-controllab + at-least-once + pending_submission_found + SecretString-or-secure-logging).
  - **`docs/triage.md` (~120 lines target):** runbook for "my MWA call is failing, what now?" Sections: (1) decision tree by symptom (no signal fires → check is_supported() and that you connected; mwa_error fires → check error code per error-codes.md; mwa_timeout fires → wallet hung or backgrounded, retry; mwa_cancelled_lifecycle fires → user rotated screen mid-op, retry); (2) error-code-to-diagnosis table (13 codes from error-codes.md mapped to "what likely happened" + "what to do"); (3) when to read `MWA.get_diagnostics()` (from Story 5-2 — counters for inflight, late-result, cleanup-failed, R8-strip-violation, etc.); (4) when to enable verbose logging (`MWA.set_verbose(true)` per AC-D-1 — the method may not yet exist on the facade; verify at T3 time and either document or file CR-5-5-A); (5) common false-positives (e.g., emulator without Fake Wallet → NO_MWA_WALLET_INSTALLED; not a real bug). Cross-reference: link to error-codes.md, signals-101.md, get_diagnostics in api-reference.md.
  - **`docs/signals-101.md` (~100 lines target):** explainer for devs new to Godot's signal model. Sections: (1) why MWA uses signals (non-blocking method-return per AC-NFR-1 ≤16ms; results arrive on Godot main thread per AC-A-1); (2) signal payload anatomy (cite arch §3.2:220 "All signals emit a single `Dictionary` argument (AC-D-10 — additive keys only)"); (3) request_id correlation (8-hex per Story 1-5 D-4; thread the same request_id from method-return → success signal → error signal → timeout signal); (4) the await + race pattern (cite REVIEW.md's `_race_signals` helper per DD-5-5-2; usage.md provides the user-facing snippet, signals-101.md explains WHY); (5) one-shot vs persistent subscriptions (`pending_submission_found` is one-shot per DD-3-3-D — link to REVIEW.md's CONNECT_ONE_SHOT pattern); (6) signal-vs-callback comparison (Godot signals are typed Dictionaries, not callback functions; type discipline is enforced at signal definition time).
  - **`docs/accessibility.md` (~80 lines target):** Sections: (1) Godot 4.3 accessibility surface (cite REVIEW.md's `Button.accessible_name` `⚠ VERSION NOT VERIFIED` marker per DD-5-5-2 + CR-5-4-B), (2) WCAG AA contrast (4.5:1 normal text / 3:1 large text — Godot default dark theme passes by-construction per REVIEW.md design guidance; custom themes need verification), (3) text-not-color rule for `mwa_error` (cite Story 5-4 AC-4 + REVIEW.md design guidance — render `payload["user_message"]` as TEXT, color is decoration), (4) keyboard navigation (Godot 4.3 Control nodes have `focus_mode` property; ensure tab-order is logical), (5) screen reader compat (Android TalkBack — Godot's accessibility track in 4.3 is incomplete; link to upstream Godot 4.3 release notes). Cross-reference: REVIEW.md (DD-5-5-2), error-codes.md (user_message field).
  - **AC-2 frontmatter** at top of each of the 4 docs.
  - **Pre-edit grep:** all 4 docs should NOT exist.
  - **Verification:** AC-1 evidence path subset (4 docs exist + ≥30 lines each); AC-7 evidence path 5 greps on safety.md (all 5 must hit); AC-2 frontmatter grep on each.
  - Maps to: AC-1 (4/13 docs), AC-2 (4 frontmatters), AC-7 (safety.md 5-topic coverage)
  - Files: `docs/safety.md` (new), `docs/triage.md` (new), `docs/signals-101.md` (new), `docs/accessibility.md` (new).
  - TDD: N/A (documentation; verification is structural greps)

- [ ] **T4 — `docs/migrations/isigner.md` AC-2 frontmatter retrofit (preserve content per DD-5-5-3).**
  - **Pre-edit confirm:** `head -1 docs/migrations/isigner.md` returns `# Migrating to \`ISigner\` (v1.0 → v1.1 → v1.2)`. `wc -l docs/migrations/isigner.md` returns 175 (or close — confirm at T4 time).
  - **Edit:** insert at the very top (before the existing `# Migrating to ISigner ...` heading):
    ```
    Owner: <user-handle>
    Drift-check: verify_docs.yml
    ```
    OR (preferred for visual consistency with future docs that may grow YAML frontmatter blocks):
    ```yaml
    ---
    Owner: <user-handle>
    Drift-check: verify_docs.yml
    ---
    ```
    Pick whichever T4 implementer chooses; both satisfy AC-2's "first 10 lines contain `Owner:` and `Drift-check:`" — confirm via the grep evidence path post-edit.
  - **Do NOT modify** any other line of the existing file.
  - **Verification:** `head -10 docs/migrations/isigner.md | grep -cE '^Owner:|^Drift-check:'` returns 2; `wc -l docs/migrations/isigner.md` returns 178 (175 + 3-line frontmatter) or 180 (175 + 5-line YAML form including `---` delimiters); diff against pre-edit shows only the inserted frontmatter (no other changes).
  - Maps to: AC-1 (1/13 docs — preservation, not creation), AC-2 (1 frontmatter)
  - Files: `docs/migrations/isigner.md` (modified — frontmatter prepend only).
  - TDD: N/A

- [ ] **T5 — `CODEOWNERS` at repo root with `docs/` entry.**
  - **NEW file:** `/Users/valentinkrumov/Documents/godot-sdk/CODEOWNERS`.
  - **Content (~10 lines target):**
    ```
    # CODEOWNERS — review ownership for godot-solana-sdk
    # See https://docs.github.com/en/repositories/managing-your-repositories-settings-and-features/customizing-your-repository/about-code-owners

    # Default catch-all — repo maintainer.
    *                                  @<user-handle>

    # Documentation surface — separate ownership for docs hygiene per Story 5-5 AC-2.
    /docs/                             @<user-handle>
    /REVIEW.md                         @<user-handle>
    /LICENSE                           @<user-handle>
    ```
  - The `<user-handle>` is set at T-time per user direction; default candidate is `@ValentinVPK` based on the active branch + git config.
  - **Path location debate:** GitHub recognizes CODEOWNERS at `/CODEOWNERS`, `/.github/CODEOWNERS`, or `/docs/CODEOWNERS`. Repo-root is most discoverable; this story chooses `/CODEOWNERS`. If T5 implementer prefers `.github/CODEOWNERS` (some teams' convention), that's acceptable — update AC-2 evidence path's grep target accordingly.
  - **Verification:** `test -f CODEOWNERS && grep -cE '^/?docs/|^/?docs ' CODEOWNERS` returns ≥1 (the `docs/` entry). AC-2 evidence path closure.
  - Maps to: AC-2 (CODEOWNERS entry)
  - Files: `CODEOWNERS` (new, repo root).
  - TDD: N/A

- [ ] **T6 — `.github/workflows/verify_docs.yml` link-check extension (AC-4).**
  - **Pre-edit:** `wc -l .github/workflows/verify_docs.yml` returns 19; the file has a single `verify` job that runs doxygen.
  - **Extension:** add a SECOND job to the workflow named `link-check`:
    ```yaml
    link-check:
      runs-on: ubuntu-latest
      steps:
        - uses: actions/checkout@v4
        - name: Run markdown link check
          uses: gaurav-nelson/github-action-markdown-link-check@v1
          # OR: lycheeverse/lychee-action@v1.10.0 — pick at T6 time based on action stability
          with:
            use-quiet-mode: 'yes'
            use-verbose-mode: 'yes'
            config-file: '.github/markdown-link-check-config.json'
            folder-path: 'docs/'
            file-path: 'README.md, REVIEW.md'
            file-extension: '.md'
    ```
  - **Optional config file `.github/markdown-link-check-config.json`** to allow-list known-flaky external URLs (e.g., GitHub Releases pages that 404 pre-tag-cut, RN package URLs):
    ```json
    {
      "ignorePatterns": [
        { "pattern": "^http://localhost" },
        { "pattern": "^https://github\\.com/.+/releases/tag/v0\\.1\\.0" }
      ],
      "timeout": "10s",
      "retryOn429": true
    }
    ```
  - **Preserve the existing `verify` doxygen job verbatim.** The new job runs in parallel; both must pass for `verify_docs.yml` to succeed.
  - **Pre-edit grep:** `grep -cE '^name: Verify Docs' .github/workflows/verify_docs.yml` returns 1; `grep -cE 'markdown-link-check|lychee' .github/workflows/verify_docs.yml` returns 0.
  - **Post-edit grep:** `grep -cE '^name: Verify Docs' .github/workflows/verify_docs.yml` returns 1 (unchanged); `grep -cE 'markdown-link-check|lychee' .github/workflows/verify_docs.yml` returns ≥1 (AC-4 closure).
  - **Smoke-validate locally:** running the link-check action requires GitHub-hosted runner; T6 ships the workflow but the actual run happens on next PR push. Local smoke alternative: `npx markdown-link-check docs/install.md` for a one-off sanity check before commit.
  - **Verification:** AC-4 evidence path (workflow has 2 jobs + link-check tool grep hits + on: pull_request preserved); first PR push exercises the gate end-to-end.
  - Maps to: AC-4
  - Files: `.github/workflows/verify_docs.yml` (modified — add `link-check` job alongside existing `verify`), optionally `.github/markdown-link-check-config.json` (new — allow-list config).
  - TDD: N/A (workflow YAML; verification is grep + first-PR-run)

- [x] **T7 — LICENSE byte-diff verification + `tools/check_license_diff.sh` helper (AC-5).** ✅ Done 2026-04-30 with **path-(1) AC-5 retargeting per A-18** (`docs/amendments.md`).
  - **A-18 amendment summary:** Story 5-5 T7 attempted the upstream-MWA-LICENSE byte-diff per the original AC-5 wording. Discovery: upstream `solana-mobile/mobile-wallet-adapter` is Apache-2.0, not MIT — local LICENSE (MIT, `Copyright (c) 2023 Axel`) shares ZERO body bytes with upstream. Spec error, not legal issue. **Path (1) resolution chosen** (4 paths considered at HALT, user-approved): retarget AC-5 to diff against OSI canonical MIT template (https://opensource.org/license/mit/) instead of upstream MWA. Helper script `tools/check_license_diff.sh` embeds OSI canonical MIT body as heredoc (no network dependency in CI); local LICENSE unchanged (already byte-matches OSI canonical MIT body); copyright `Copyright (c) 2023 Axel` preserved per Step C option 1 (first-author lineage). CR-5-5-D filed in `docs/concerns.md`.
  - **Pre-edit (PRE-A-18, retained for lineage):** `wc -l LICENSE` returns 22; first line `MIT License`; line 3 `Copyright (c) 2023 Axel`. Body (lines 5-22) is standard MIT permission text.
  - **Step A — fetch upstream:** `curl -s https://raw.githubusercontent.com/solana-mobile/mobile-wallet-adapter/main/LICENSE > /tmp/upstream-mwa-license.txt`.
  - **Step B — byte-diff bodies (excluding copyright lines):** `diff <(grep -v '^Copyright' /tmp/upstream-mwa-license.txt) <(grep -v '^Copyright' LICENSE)`. Three outcomes:
    1. Empty diff → LICENSE body conforms; no rewrite needed. Proceed to Step D.
    2. Non-empty diff with cosmetic differences (trailing whitespace, line-ending CRLF vs LF) → normalize and re-diff. If still empty after normalization, conform.
    3. Non-empty diff with substantive differences (different MIT body wording) → rewrite local LICENSE body to upstream verbatim, preserving the local copyright-holder line.
  - **Step C — copyright-holder policy:** ASK USER at T7 time. Default candidates:
    - Keep `Copyright (c) 2023 Axel` unchanged (project's first-author lineage).
    - Add a second line `Copyright (c) 2024-2026 LimeChain Tech / godot-solana-sdk MWA contributors`.
    - Replace entirely with `Copyright (c) 2024-2026 godot-solana-sdk contributors` (consolidates).
  - **Step D — ship `tools/check_license_diff.sh`:**
    ```bash
    #!/usr/bin/env bash
    # tools/check_license_diff.sh — verifies LICENSE body matches upstream solana-mobile/mobile-wallet-adapter MIT
    # excluding the copyright-holder line. Used locally by Story 5-5 T7 + by Story 5-6 PR-blocking CI gate.
    set -euo pipefail
    UPSTREAM_URL="https://raw.githubusercontent.com/solana-mobile/mobile-wallet-adapter/main/LICENSE"
    UPSTREAM_FILE=$(mktemp)
    trap 'rm -f "$UPSTREAM_FILE"' EXIT
    curl -fsSL "$UPSTREAM_URL" > "$UPSTREAM_FILE"
    if diff <(grep -v '^Copyright' "$UPSTREAM_FILE") <(grep -v '^Copyright' LICENSE); then
        echo "PASS: LICENSE body byte-identical to upstream (excluding copyright-holder line)."
        exit 0
    else
        echo "FAIL: LICENSE body diverges from upstream. See diff above." >&2
        exit 1
    fi
    ```
    Make executable: `chmod +x tools/check_license_diff.sh`.
  - **Step E — run locally:** `bash tools/check_license_diff.sh` should exit 0. If it exits non-zero, return to Step B.
  - **Pre-edit grep:** `test -f tools/check_license_diff.sh` should fail (T7 creates it).
  - **Post-edit grep:** `test -x tools/check_license_diff.sh` should pass; `bash tools/check_license_diff.sh` exits 0.
  - **Verification:** AC-5 closure — the helper script exists, is executable, and exits 0 against the committed LICENSE; PR-blocking CI gate ships in Story 5-6.
  - Maps to: AC-5
  - Files: `LICENSE` (potentially modified — body rewrite OR copyright-line tweak per Step C), `tools/check_license_diff.sh` (new).
  - TDD: N/A

- [ ] **T8 — Pre-Gate-5 close-out: link-check sweep + frontmatter audit + AC-1 substantive-content audit + state.json + sprint-status + concerns sweep.**
  - **Step A — AC-1 EXISTS audit:** `for f in install usage api-reference error-codes compatibility-matrix naming-map rn-parity api-stability safety triage signals-101 accessibility; do test -s "docs/$f.md" || echo "MISSING: docs/$f.md"; done; test -s "docs/migrations/isigner.md" || echo "MISSING: docs/migrations/isigner.md"` → expect zero `MISSING:` lines. Plus `wc -l` per file: each ≥30 (≥20 for rn-parity.md and naming-map.md).
  - **Step B — AC-2 frontmatter audit:** `for f in $(ls docs/*.md docs/migrations/isigner.md); do count=$(head -10 "$f" | grep -cE '^Owner:|^Drift-check:'); test "$count" -ge 2 || echo "FRONTMATTER MISSING: $f ($count/2)"; done` → zero `FRONTMATTER MISSING:` lines for any 5-5-touched doc. (Skip pre-existing docs not in 5-5 scope: amendments.md / architecture.md / concerns.md / plan.md / research.md / spec.md / Godot_MWA_Task_Overview.md / sprint-status.yaml — these are LaiM artifacts, NOT in AC-1's 13-doc list.)
  - **Step C — AC-3 method coverage audit:** `grep -cE '^### MWA\.(connect|disconnect|reauthorize|deauthorize|sign_messages|sign_transactions|sign_and_send|forget_all|is_supported|is_connected|get_public_key|get_cluster|get_wallet_label|get_diagnostics|get_device_posture)' docs/api-reference.md` returns ≥15. `grep -cE '^```gdscript' docs/api-reference.md` returns ≥15.
  - **Step D — AC-7 safety.md 5-topic audit:** run all 5 greps from AC-7 evidence path (FLAG_SECURE / attacker-controllab / at-least-once / pending-submission / SecretString); all 5 must hit ≥1.
  - **Step E — AC-4 link-check local smoke:** `npx markdown-link-check docs/install.md docs/usage.md docs/api-reference.md REVIEW.md` (requires Node + npm; if not available locally, skip and rely on next-PR CI run). Capture any broken-link findings; fix or allow-list as appropriate.
  - **Step F — AC-5 LICENSE byte-diff sanity:** `bash tools/check_license_diff.sh` exits 0.
  - **Step G — AC-6 rn-parity validator:** `python tools/validate_rn_parity.py` exits 0.
  - **Step H — REVIEW.md cross-reference audit (DD-5-5-2 enforcement):** `grep -cE 'REVIEW\.md' docs/install.md docs/usage.md docs/api-reference.md docs/accessibility.md docs/safety.md` returns ≥5 (one per doc; some may have multiple).
  - **Step I — Test baseline preservation:** `./gradlew -p android :plugin:testDebugUnitTest && .venv/bin/pytest tools/` should remain at 277 GREEN. 5-5 has zero source surface; any deviation is a baseline regression — investigate per VERIFY.md §3d.1. Update state.json `baselineTests.lastRefreshedAt` to current ISO timestamp; `lastRefreshedAtStory: 20`; `failingTests` should remain `[]`.
  - **Step J — sprint-status.yaml:** 5-5 status → done, completedAt timestamp, postTag `post-mwa-android-5-5`, gate5Result pass.
  - **Step K — state.json `completedStories[]`:** add Story 5-5 entry following the Story 5-3 / 5-4 templates. Notes cite: (a) 13 markdown docs landed (12 new + 1 isigner.md frontmatter retrofit per DD-5-5-3); (b) LICENSE conformance verified via tools/check_license_diff.sh per DD-5-5-5 (AC-5); (c) verify_docs.yml extended with link-check job (AC-4); (d) CODEOWNERS landed at repo root with `docs/` entry (AC-2); (e) tools/validate_rn_parity.py + tools/check_license_diff.sh helper scripts shipped (AC-5/6 source-of-truth; CI gates wired in Story 5-6 per DD-5-5-1); (f) ZERO production code changes — Kotlin / C++ / GDScript / JNIEXPORT untouched; (g) baseline 277 GREEN preserved (no source-surface delta); (h) FR-15 covered (plan.md:490); (i) NFR-11 covered (LICENSE byte-diff per spec.md:174 AC-NFR-7); (j) AC-NFR-SEC-12 docs surface landed (FLAG_SECURE + identity-metadata phishing in safety.md per AC-7); (k) DD-5-5-1..5 all 5 LOCKED.
  - **Step L — D-5-5-* deviation logging** — append to `docs/concerns.md` under `### Story 5-5 deviations`. Expected entries: any T-time Rule-1/Rule-2 adjustments (e.g., D-5-5-T2-1 if rn-parity.md schema extended to 8 entries because clientlib-ktx 2.0.3 has a forget_all-equivalent; D-5-5-T7-1 if LICENSE body rewrite was needed; D-5-5-T6-1 if link-check action choice (lychee vs markdown-link-check) needed switching post-failed-CI-run).
  - **Step M — CR sweep:**
    - **CR-5-4-A (LOW, MWA.clear_local_cache facade gap):** UNCHANGED — 5-5's docs (api-reference.md + REVIEW.md cross-reference) document the current `MWA.disconnect()` standand; CR-5-4-A still tracks the eventual facade addition in a future minor version.
    - **CR-5-4-B (LOW, Button.accessible_name version not verified):** UNCHANGED — 5-5's accessibility.md cross-references REVIEW.md's `⚠ VERSION NOT VERIFIED` marker; the verification itself is OUT-OF-LAIM-SCOPE per A-17 (external repo's CI verifies).
    - **CR-5-4-C (LOW, REVIEW.md release URL placeholder):** UNCHANGED until Story 5-6 release-on-tag CI lands.
    - **CR-5-4-D (MEDIUM, instrumented_emulator_fakewallet.yml redesign):** UNCHANGED — Story 5-6 owns.
    - **CR-5-4-E (HIGH, external repo CI gap):** UNCHANGED — user-owned post-Gate-5.
    - **CR-5-4-F (MANDATED, release-on-tag CI):** UNCHANGED — Story 5-6 owns.
    - **CR-5-4-G (HIGH, C++ compile blocker):** UNCHANGED — Story 5-6 confronts.
    - **CR-5-5-A (NEW, LOW)** — `MWA.set_verbose(true)` (per AC-D-1) may not exist on the facade. Triage.md references it; if it's missing, file an amendment OR remove the reference. Implementer verifies at T3 time. Tracked as CR-5-5-A.
    - **CR-5-5-B (NEW, LOW)** — RN parity row count: clientlib-ktx 2.0.3 may expose a 7th-or-8th method that the godot-mwa surface does not implement (e.g., `getCapabilities` or a similar capability-discovery RPC). If so, the YAML may need an 8th row marking that method `gdscript_method: "<not implemented>"` so the cron picks up the gap. Implementer audits at T2 time. Tracked as CR-5-5-B.
    - **CR-5-5-C (NEW, LOW)** — `verify_parity.yml` cron schedule + npm-fetch logic + `gh issue create --label parity-drift` is non-trivial; if Story 5-6's authors find the AC-3 contract under-specified, they may need to revisit DD-5-5-4 schema. Tracked as a risk for 5-6's design phase.
  - **Pre-Gate-5 close-out commit:** `chore(mwa): Story 5-5 T8 close-out — 13 docs + LICENSE + CODEOWNERS + verify_docs link-check + helper scripts; baseline 277 GREEN preserved; CR-5-5-A/B/C added`.
  - **Verification:** `git status` clean; `git tag -l "pre-mwa-android-5-5" "post-mwa-android-5-5"` shows the pre-tag (post-tag added by orchestrator on Gate 5 PASS); all 7 ACs evidence paths pass; all 8 must_haves below pass.
  - Maps to: n/a (close-out)
  - Files: `docs/state.json` (modified — completedStories + baselineTests refresh), `docs/sprint-status.yaml` (modified — 5-5 done), `docs/concerns.md` (modified — D-5-5-* deviations + CR-5-5-A/B/C added).

## must_haves

EXISTS + SUBSTANTIVE + WIRED checks for goal-backward verification at Gate 5:

1. **All 13 docs exist with non-empty content** (T1+T2+T3+T4; AC-1). `for f in install usage api-reference error-codes compatibility-matrix naming-map rn-parity api-stability safety triage signals-101 accessibility; do test -s "docs/$f.md" || echo MISSING; done; test -s "docs/migrations/isigner.md" || echo MISSING` produces ZERO lines AND `wc -l` for each is ≥30 (≥20 for rn-parity.md and naming-map.md).
2. **All 13 docs have `Owner:` + `Drift-check:` frontmatter in first 10 lines** (T1+T2+T3+T4; AC-2). `for f in install usage api-reference error-codes compatibility-matrix naming-map rn-parity api-stability safety triage signals-101 accessibility; do head -10 "docs/$f.md" | grep -cE '^Owner:|^Drift-check:' | grep -v '^2$' && echo BAD; done; head -10 docs/migrations/isigner.md | grep -cE '^Owner:|^Drift-check:'` returns 2 for each (zero `BAD` lines).
3. **CODEOWNERS exists with `/docs/` entry** (T5; AC-2). `test -f CODEOWNERS && grep -cE '^/?docs/|^/?docs ' CODEOWNERS` returns ≥1.
4. **api-reference.md covers ≥15 methods with ≥15 GDScript snippets** (T1; AC-3). `grep -cE '^### MWA\.(connect|disconnect|reauthorize|deauthorize|sign_messages|sign_transactions|sign_and_send|forget_all|is_supported|is_connected|get_public_key|get_cluster|get_wallet_label|get_diagnostics|get_device_posture|get_suggested_wallet_install_links|get_auth_token_fingerprint)' docs/api-reference.md` returns ≥15; `grep -cE '^```gdscript' docs/api-reference.md` returns ≥15.
5. **verify_docs.yml has link-check job** (T6; AC-4). `grep -cE 'link-check' .github/workflows/verify_docs.yml` returns ≥1; `grep -cE 'markdown-link-check|lychee' .github/workflows/verify_docs.yml` returns ≥1; original `verify` (doxygen) job preserved (`grep -cE 'doxygen' .github/workflows/verify_docs.yml` returns ≥1).
6. **LICENSE byte-diff helper exists and exits 0** (T7; AC-5). `test -x tools/check_license_diff.sh && bash tools/check_license_diff.sh; echo $?` returns `PASS:` line + exit code 0.
7. **rn-parity.md YAML has 7 entries × 6 keys; validator exits 0** (T2; AC-6). `python tools/validate_rn_parity.py docs/rn-parity.md; echo $?` returns exit 0; OR if positional-arg variant used, `python tools/validate_rn_parity.py; echo $?` returns 0. `grep -cE '^- rn_method:' docs/rn-parity.md` returns 7.
8. **safety.md hits all 5 AC-7 topic greps** (T3; AC-7). `grep -cE 'FLAG_SECURE' docs/safety.md` ≥1; `grep -cE 'attacker-controllab|attacker.*identityName|identityName.*attacker' docs/safety.md` ≥1; `grep -cE 'at-least-once' docs/safety.md` ≥1; `grep -cE 'pending_submission_found|pending-submission' docs/safety.md` ≥1; `grep -cE 'SecretString|SdkLog|R8.*strip|secure logging' docs/safety.md` ≥1.
9. **REVIEW.md cross-reference enforcement (DD-5-5-2)** (T1+T3; design discipline). `grep -cE 'REVIEW\.md' docs/install.md docs/usage.md docs/api-reference.md docs/accessibility.md docs/safety.md` returns ≥5 (≥1 per doc).
10. **isigner.md preservation (DD-5-5-3)** (T4). `wc -l docs/migrations/isigner.md` returns 178-180 (175 + 3-line OR 5-line YAML frontmatter); `grep -c '^# Migrating to' docs/migrations/isigner.md` returns 1; `grep -c '## 5\. Reference' docs/migrations/isigner.md` returns 1 (preserved §5 section).
11. **Baseline test count remains 277 GREEN post-T8** (close-out; no regression). `state.json baselineTests.failing == 0` AND `baselineTests.total == 277` (T8 Step I).

## Out of Scope (explicit non-goals)

- **`verify_parity.yml` weekly cron workflow** — Story 5-6 AC-3 owns. 5-5 ships the YAML data source + validator script per DD-5-5-1.
- **`byte_diff_license.yml` PR-blocking CI workflow** — Story 5-6 owns. 5-5 ships the helper script per DD-5-5-1.
- **`naming_map_drift.yml` AC-D-9 CI drift check** — Story 5-6 owns (likely extends `codegen_drift_check.yml`). 5-5 ships the source-of-truth doc.
- **`compat_matrix_drift.yml` AC-D-7 CI drift check** — Story 5-6 AC-8 owns. 5-5 ships the source-of-truth doc.
- **`@since` XML tag automation** — Story 5-6 AC-7 owns (`scripts/check_since_tags.py`). 5-5 documents the policy in api-stability.md.
- **R8 keep rules / consumer-rules.pro updates** — no R8 surface change; 5-5 is documentation-only.
- **Source code changes (Kotlin / C++ / GDScript / JNIEXPORT)** — 5-5 has zero production-code surface. Any such change triggers VERIFY.md §3d.1 baseline-comparison.
- **README.md rewrite** — README at repo root is OUT-OF-SCOPE for 5-5 (5-5's docs live in `docs/` per AC-1's file list). README touches happen in Story 5-6 per AC-1's "5-line `Build the addon` section" + AC-D-11 + DD-5-5-2 cross-reference (README links to install.md for in-depth setup).
- **REVIEW.md edits** — REVIEW.md is the Story 5-4 deliverable; 5-5 does NOT modify it (DD-5-5-2 cross-references; does not edit). If 5-5 finds REVIEW.md content that needs correction, file CR-5-5-D rather than editing in-place.
- **External repo (`ValentinVPK/godot-mwa-example`)** — OUT-OF-LAIM-SCOPE per A-17. 5-5's docs reference the external repo's existence but do not document its internals.
- **Promoting `v0.1.0-rc1` → `v0.1.0`** — Story 5-6 release-on-tag CI cuts the first stable release. 5-5's api-stability.md documents the policy; the actual cut is 5-6 work.
- **Storybook / interactive doc site** — text + markdown only. Doc-site generation (Doxygen, MkDocs, Docusaurus) is OUT-OF-SCOPE; the existing `verify_docs.yml` doxygen step is preserved but not extended.
- **Changelog / release-notes generation** — `CHANGELOG.md` may be touched by Story 5-6 release-on-tag CI; not 5-5.
- **Translation / i18n** — English only.

## Concerns

- **CR-5-5-A (NEW, LOW)** — `MWA.set_verbose(true)` may not exist on the facade. AC-D-1 (spec.md:152) describes it; if missing from MWA.gd, the triage.md reference is forward-looking. Implementer verifies at T3 time and either documents the gap or removes the reference. Mitigation: future minor version adds the facade method.
- **CR-5-5-B (NEW, LOW)** — RN parity row count uncertainty. clientlib-ktx 2.0.3 may expose 7 or 8 op methods (`getCapabilities` is a candidate 8th). The 7-row YAML schema in DD-5-5-4 reflects the 7 godot-mwa-implemented ops; if clientlib-ktx has more, an 8th row with `gdscript_method: "<not implemented>"` may be needed so the cron picks up the gap. Tracked for T2-time audit.
- **CR-5-5-C (NEW, LOW)** — `verify_parity.yml` cron design risk. Story 5-6 AC-3 mandates Sunday 03:00 UTC + npm-pinned-package diff + `gh issue create --label parity-drift` on new method. If 5-6's authors find DD-5-5-4's schema under-specified (e.g., need a `last_verified_at` field for staleness detection), they may need to revisit the schema. Tracked as a risk for 5-6's design phase.

## Detected Patterns

| Pattern | Value | Sampled from | Established? |
|---------|-------|-------------|-------------|
| Story file structure (frontmatter + Context + AC + Architecture Guardrails + Verified Interfaces + Tasks + must_haves + Out of Scope + Concerns + Detected Patterns + Dev Notes) | matches Story 5-3 (382 lines) and Story 5-4 (213 lines, REDUCED) | `docs/stories/5-3.md`, `docs/stories/5-4.md` | ✅ Established (Wave 5+ convention) |
| AC-2 frontmatter shape `Owner: <handle>` + `Drift-check: <workflow>` | NEW per Story 5-5 — first time landed in this codebase | arch:1214 maintainer review #4 | ⚠ NEW (single-source; this story locks it; Stories 5-6 + future docs inherit) |
| REVIEW.md cross-reference (`./REVIEW.md` from docs/, `REVIEW.md` from repo root) | NEW per Story 5-5 — cross-reference rather than duplication | DD-5-5-2 | ⚠ NEW (DD-5-5-2 LOCKS) |
| YAML-in-markdown machine-readable section (` ```yaml ... ``` ` block extracted by validator script) | matches `error-codes.yaml` pattern + `gradle.lockfile` pattern (textual + parsed) | arch:319 + DD-5-5-4 | ✅ Established (codegen pattern from Story 1-1) |
| Helper-script-in-5-5 + CI-gate-in-5-6 divide | matches Story 5-3 / 5-4 / 5-1 pattern of "data first, CI second" | DD-5-5-1; arch §10.4 CI cluster | ✅ Established (Wave 7 convention) |
| Documentation-only story baseline preservation | matches Story 5-4 (no Kotlin / C++ surface) | Story 5-4 T2 (post-A-17) | ✅ Established (Wave 7 docs-heavy stories) |

## Dev Notes

### Sizing audit conclusion

M is on the line — large deliverable surface (13 docs + LICENSE + workflow + CODEOWNERS + 2 helper scripts + rn-parity YAML), but each individual deliverable is small (~50-150 line docs of straightforward content; the LICENSE byte-diff is mechanical; the YAML schema is 7 rows × 6 keys). Comparable to Story 5-3's S-with-justification (382 lines, 5 LOCKED DDs across 4 prior systems). 5-5 has 5 LOCKED DDs and similar story-file density, so M is acceptable IF the implementer:
- Uses REVIEW.md (208 lines) as authoritative source for example-app guidance — DOES NOT re-derive (DD-5-5-2 enforcement).
- Uses architecture.md §3.2/§3.3 as authoritative source for signal payloads + error codes — cites by line range, DOES NOT inline.
- Uses MWA.gd:27-418 as the single source of truth for the API surface — DOES NOT speculate.
- Preserves isigner.md's existing 175 lines per DD-5-5-3.

If any task expands beyond ~2 hr at T-time, file an amendment proposing L bump. Track scope-creep in T8's deviation sweep.

### Cross-references for the implementer

- **REVIEW.md (post-5-4 T1, 208 lines, repo root):** canonical home for `_race_signals` helper / PASS marker shape / mainnet gate semantics / `Button.accessible_name` `⚠ VERSION NOT VERIFIED` / AC-1 9-button list / state-transition log shape / 17-row API surface table. DD-5-5-2 enforces cross-reference (NOT duplication) in install.md, usage.md, api-reference.md, accessibility.md, safety.md.
- **`docs/architecture.md` §3.2:218-298** — signal payload schemas (12 signals × Dictionary keys). api-reference.md + signals-101.md cite by line range; do NOT inline.
- **`docs/architecture.md` §3.3:316-409** — error-codes.yaml manifest (13 codes). error-codes.md DERIVES content (per-code subsection with enum name, recoverable, retry_hint, default_user_message, source_method values). Do NOT regenerate the manifest; just consume.
- **`docs/architecture.md` §2.2:71-85** — Container View 13-row component table. naming-map.md cites for the canonical Component column.
- **`docs/amendments.md` A-12** — 1-param mwa_cancelled_lifecycle Dictionary contract.
- **`docs/amendments.md` A-14** — mwa_error 9-key payload canonical (note: spec.md historical references to 10-key are pre-A-14; A-14 is final shape).
- **`docs/amendments.md` A-17** — architectural relocation of example app to `ValentinVPK/godot-mwa-example` external repo + REVIEW.md as canonical reviewer surface.
- **`addons/SolanaSDK/mwa/MWA.gd:27-47`** — 12 signals.
- **`addons/SolanaSDK/mwa/MWA.gd:133-418`** — 14 methods. api-reference.md AC-3 covers all 14 (7 ops + forget_all + is_supported + 6 state getters = 15 if you count get_auth_token_fingerprint; but is_connected/get_public_key/get_cluster/get_wallet_label/get_auth_token_fingerprint/get_diagnostics/get_device_posture/get_suggested_wallet_install_links = 8 state-getters/utility, plus 7 ops + forget_all + is_supported = 17 total; 15+ methods is the AC-3 floor).
- **`docs/stories/5-3.md` + `docs/stories/5-4.md`** — most recent Wave 7 story templates; 5-5 matches their structure.
- **`android/plugin/build.gradle.kts:35,46,47,225`** — compileSdk=34, minSdk=23, targetSdk=34, clientlib-ktx=2.0.3. compatibility-matrix.md cites verbatim.
- **`godot-solana-sdk.gdextension:4`** — `compatibility_minimum = "4.3"` post-DD-5-4-1 fix. compatibility-matrix.md cites.

### Pre-implementation user-led audit checklist (run BEFORE T1 commit)

- [ ] Verify pre-tag exists: `git tag -l 'pre-mwa-android-5-5'` → `pre-mwa-android-5-5`
- [ ] Verify post-5-4 baseline: `wc -l docs/stories/5-4.md` → 213; `grep gate5Result docs/sprint-status.yaml | head -10` → 5-4 done with `pass`
- [ ] Verify REVIEW.md exists at repo root (208 lines, post-5-4 T1 deliverable): `wc -l REVIEW.md`
- [ ] Verify isigner.md exists with substantive content (175 lines, Story 1-3 deliverable): `wc -l docs/migrations/isigner.md && head -1 docs/migrations/isigner.md`
- [ ] Verify LICENSE exists at repo root (22 lines, "Copyright (c) 2023 Axel"): `wc -l LICENSE && head -3 LICENSE`
- [ ] Verify .github/workflows/verify_docs.yml exists (19 lines, doxygen-only): `wc -l .github/workflows/verify_docs.yml && grep -c doxygen .github/workflows/verify_docs.yml`
- [ ] Verify CODEOWNERS does NOT exist: `test -f CODEOWNERS || test -f .github/CODEOWNERS && echo EXISTS || echo MISSING` → MISSING
- [ ] Verify the other 12 AC-1 docs do NOT exist: `for f in install usage api-reference error-codes compatibility-matrix naming-map rn-parity api-stability safety triage signals-101 accessibility; do test -f "docs/$f.md" && echo "UNEXPECTED EXISTS: $f"; done` → zero `UNEXPECTED EXISTS:` lines
- [ ] Verify MWA.gd API surface signature snapshot: `grep -cE '^(func|signal) ' addons/SolanaSDK/mwa/MWA.gd` → 26 (12 signals + 14 funcs at lines 27-418)
- [ ] Verify build.gradle.kts compat values: `grep -nE 'compileSdk|minSdk|targetSdk|clientlib-ktx' android/plugin/build.gradle.kts | head -5`
- [ ] Verify .gdextension compatibility_minimum: `grep -n 'compatibility_minimum' godot-solana-sdk.gdextension` → `4:compatibility_minimum = "4.3"` (post-DD-5-4-1)
- [ ] Read REVIEW.md sections 8 ("Design guidance for the example-repo implementer") + 9 ("API surface (consumed by the example repo)") for DD-5-5-2 cross-reference targets.

If ANY check fails → STOP. State drift detected; surface to user.

### Story-creation provenance

Authored by story-creator-agent on 2026-04-29 (post-5-4 Gate 5 PASS). Pre-tag `pre-mwa-android-5-5` at HEAD `39ff0d1f` (sprint-status:216 + state.json bookkeeping). 5 LOCKED design decisions. 3 LOW concerns NEW. 8 tasks (T1 foundation cluster, T2 compat cluster, T3 safety cluster, T4 isigner.md retrofit, T5 CODEOWNERS, T6 verify_docs.yml link-check, T7 LICENSE byte-diff + helper, T8 close-out). ZERO source code surface. ZERO new amendments. Closes ZERO concerns. Adds CR-5-5-A/B/C as LOW NEW concerns.

Live state confirmed at story-creation:
- post-5-4 Gate 5 PASS at `dd05abc5` + `0e10a495` + `046a5a00` (Wave 7 4/6 complete)
- baseline 277 GREEN (lastRefreshedAt 2026-04-29T15:30Z post-5-4 close-out per state.json:988-998)
- sprint-status.yaml:208-216 — Story 5-5 status=in-progress, baselineTag=pre-mwa-android-5-5, startedAt 2026-04-29T15:35:00Z
- pre-tag `pre-mwa-android-5-5` at `39ff0d1f` (commit message: "chore(mwa): pause Story 5-4 at story-creation checkpoint (fresh /start resume)")
- Wave 7 progress: 5-1 ✅, 5-2 ✅, 5-3 ✅, 5-4 ✅ (REDUCED-SCOPE per A-17), 5-5 in-progress (THIS), 5-6 backlog
- AC-1 disk audit: 12 NEW docs + 1 EXISTING (isigner.md, 175 lines preserved) + 1 EXISTING (LICENSE, 22 lines, byte-diff at T7) + 1 EXISTING extended (verify_docs.yml, 19 lines, link-check job added at T6) + 1 NEW (CODEOWNERS at T5) + 2 NEW helper scripts (validate_rn_parity.py at T2, check_license_diff.sh at T7).

### Story-creation acceptance checklist (T8 must satisfy at PR time)

1. ✅ All 13 AC-1 docs exist with non-empty content (≥30 lines except rn-parity / naming-map ≥20)
2. ✅ All 13 docs + isigner.md have `Owner:` + `Drift-check:` frontmatter in first 10 lines
3. ✅ CODEOWNERS at repo root with `docs/` entry
4. ✅ api-reference.md covers ≥15 methods + ≥15 GDScript snippets (AC-3)
5. ✅ verify_docs.yml has `link-check` job alongside preserved `verify` doxygen job (AC-4)
6. ✅ LICENSE body byte-identical to upstream solana-mobile/mobile-wallet-adapter (excluding copyright-holder line); `tools/check_license_diff.sh` exits 0 (AC-5)
7. ✅ rn-parity.md has 7 entries × 6 keys YAML; `tools/validate_rn_parity.py` exits 0 (AC-6)
8. ✅ safety.md hits all 5 AC-7 topic greps (FLAG_SECURE / attacker-controllab / at-least-once / pending_submission_found / SecretString-or-secure-logging)
9. ✅ DD-5-5-2 cross-reference enforcement: ≥5 REVIEW.md mentions across install/usage/api-reference/accessibility/safety
10. ✅ DD-5-5-3 isigner.md preservation: 175 lines + 3-5 line frontmatter prepend; §5 Reference section preserved
11. ✅ Baseline 277 GREEN preserved (no source surface delta)
12. ✅ T8 close-out: state.json + sprint-status.yaml updated; CR-5-5-A/B/C added; D-5-5-* deviations logged
