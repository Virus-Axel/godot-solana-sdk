# Story 4-3 T6 — synthetic consumer subprojects build outputs.
# These are generated by `./gradlew :synthetic-consumer:assembleRelease` /
# `:synthetic-warning:assembleRelease` (see manifest_merge_audit.yml).
# Mirrors android/.gitignore conventions.
build/
.gradle/
*.iml
.idea/
local.properties
